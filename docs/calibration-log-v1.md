# 校准试验台账 v1

> 记录**每一次**跑过的配置，**包括失败的**，以及已被"消耗"的数据时段。不记录失败次数的回测报告不可信。
>
> 本台账是追加式的：只增不改，更正请新增一条并引用原条目。

---

## 一、已消耗的数据时段（所有测试折必须晚于它们）

| # | 时段（UTC，左闭右开） | 用途 | 消耗规则 |
|---|---|---|---|
| W1 | **2025-09-01 00:00 → 2025-12-01 00:00**（91 天，2184 根 1h） | 区间判别参数测量（`docs/feature-spec-v1.md`「区间判别参数」） | **任何最终测试折的起点必须晚于 2025-12-01 00:00 UTC。** 该时段的特征分布已被用来选参数，不能再充当无偏的测试数据 |

⚠️ W1 只被用于**特征侧**分布测量，没有接触任何标签、收益或前瞻结果。但参数选择用到了该时段的分布，因此按规则仍视为已消耗。

---

## 二、试验记录

### T1 · 区间判别参数的老窗口测量（2026-09-20）

**目的**：为 `detectRange` 的 `rangeMaxHeightAtr` / `rangeTolAtr` / `zigzagAtrMult` 及新增的语义判据（包含率、触碰次数）提供数据依据，取代此前无依据的临时值。

**数据**
- 来源：`data.binance.vision` 官方公开数据仓库，`futures/um/monthly/klines/<SYMBOL>/1h/<SYMBOL>-1h-2025-{09,10,11}.zip`
- 完整性：每个压缩包均与其同目录 `.CHECKSUM` 的 SHA256 比对通过（60/60）
- 规模：20 个币 × 2184 根 = 43,680 根；无缺口；合计下载约 1.95MB
- 为什么不走实时 API：执行当天 `fapi.binance.com` / `api.binance.com` 从本机直连均为连接超时（`UND_ERR_CONNECT_TIMEOUT`），公开数据仓库可达。数据列格式与 `/fapi/v1/klines` 一致，走同一个 `toBars()` 入口

**选币（无挑选）**：UM · USDT · 永续 · 上市早于 2025-08-01 的合约（当前列表中共 379 个），按 24h 成交额降序，等距抽取 20 个：

`ARBUSDT STRKUSDT SYNUSDT AEROUSDT ZKUSDT MUBARAKUSDT ENSUSDT DEXEUSDT SPKUSDT GRIFFAINUSDT PARTIUSDT CATIUSDT DOGSUSDT MERLUSDT SSVUSDT AXLUSDT CELRUSDT SAFEUSDT MAVUSDT RPLUSDT`

**方法**
- 测量时**不设高度上限**（`maxHeightAtr = Infinity`），避免只看得到切点以内的区间
- 评估点：每 6 根取一个（步长 6），从第 182 根起（168 回看 + 14 ATR 预热），每配置 6680 个 (币, 时点)
- 摆动点：ZigZag（ATR 阈值）；`lookback = 168`；`minSwings = 4`
- 扫描的配置：`zigzagAtrMult ∈ {1.5, 2, 3}` × `rangeTolAtr ∈ {0.3, 0.5, 1.0}` = **9 组**
- 每组统计：`heightAtr`、包含率、上下沿触碰次数、窗口内 swing 数的 p10/p25/p50/p75/p90；覆盖率曲线（高度切点 × 包含率阈值 × 触碰下限）
- 特征侧测量，**不涉及任何标签、Brier 技能分或收益**，没有可报告的 BSS

**结果**：见 `docs/feature-spec-v1.md`「区间判别参数」（由架构会话定值后写入）。

**已知局限**
- 幸存者偏差：候选币取自**当前**仍在交易的合约，已下架的币拿不到（无法修复，见交接文档第八节陷阱 8）
- 仅 20 个币、单一 91 天窗口、单一市场区间；分布可能随 regime 变化（该窗口内按月的参考切点覆盖率为 9 月 0.356 / 10 月 0.344 / 11 月 0.318，月间较稳定，但只有三个月）
- 抽样偏向中小市值币（按成交额等距抽取，不含 BTC/ETH 等头部），符合"早期币"的目标场景，但头部币的分布未测

### T2 · 区间参数冻结与测量脚本入库（2026-09-20）

**冻结值**（架构裁定，权威来源为 `docs/feature-spec-v1.md`；代码在 `lib/structure/analyze.ts` 的 `STRUCTURE_PARAMS`）

| 参数 | 值 | 备注 |
|---|---|---|
| `zigzagAtrMult` | 2 | T1 中 mult=2 在三个 tol 下都最稳 |
| `rangeTolAtr` | 0.5 | tol=1.0 只多约 9 个百分点覆盖，但 1 ATR 的"同一水平"过松 |
| `rangeLookback` | 168 | **T1 未扫描此维度，依据最弱**，留作后续敏感性检查 |
| `rangeMinSwings` | 4 | 来自 feature-spec（有效摆动点 < 4 ⇒ 缺失） |
| `rangeMinTouches` | 2 | 结构性下限（定义"水平"），不作判别 |
| `rangeMaxHeightAtr` | 25 | 纯安全阀；T1 中 cap 25 与 ∞ 覆盖率相差 0.0001 |
| 包含率阈值 | 不设 | 包含率改为特征 `f3_range_containment`（0–1） |

**语义变更**：检测结果改为三态。`range`（has_range）/ `no_range`（数据充足但无相干边界，是真实状态，特征值 0，**不缺失**）/ `unavailable`（数据不足，唯一的缺失）。T1 的原始覆盖率 33.8% 来自把包含率当门槛；改为特征后 has_range 覆盖率 = **85.3%**（cap 25、触碰 ≥ 2）。

**T1 数字的一处说明**：先前回报的 `heightAtr`/触碰分布（p50 = 3.6 等）包含"无满足触碰数的簇时退回窗口极值、触碰=1"的点。冻结后的实现**没有**这个回退（单次触碰不是水平）。触碰 ≥ 2 的覆盖率表两者逐格一致；仅 has_range 点的 `heightAtr` 分布因此略有变化（p50 3.6 → 3.2，p90 12.0 → 8.3；包含率 p50 0.55 → 0.53）。

**T2 复现结果（新实现，mult=2 / tol=0.5，6680 点）**
- 状态占比：range 85.3% / no_range 14.7% / unavailable 0%
- no_range 原因：上沿无 ≥2 次触碰的簇 471、下沿无 ≥2 次触碰的簇 278、上沿簇不高于下沿簇 233
- has_range（cap 25）按月：9 月 0.874 / 10 月 0.825 / 11 月 0.866
- has_range 的 `heightAtr` p10/p25/p50/p75/p90 = 1.48 / 2.17 / 3.24 / 5.09 / 8.26；包含率 = 0.27 / 0.38 / 0.53 / 0.71 / 0.86；swing 数 = 15 / 16 / 18 / 21 / 22
- 参数敏感性（has_range 覆盖，cap 25、触碰 ≥ 2）：mult 1.5 → 89.2 / 93.3 / 96.0%；mult 2 → 72.4 / **85.3** / 94.7%；mult 3 → 23.4 / 38.5 / 61.6%（tol = 0.3 / 0.5 / 1.0）

**可复现**
- 脚本：`scripts/measure-range-params.mjs`（只用特征侧，不碰标签）
- 输入文件 `klines-1h.json`（由 T1 的 60 个官方压缩包合并而成）：6,139,237 字节，SHA256 `8b1bca6054167b2aed243578e30171925164655eb16f62a57623efb523a6fe87`
- 运行：`node scripts/measure-range-params.mjs <klines-1h.json> [out.json]`
- 输入文件本身不入库（体积与再分发考虑）；来源、币单、时段见 T1，按同样规则可重建

**待架构裁定的开放问题**：干净的单边趋势不会出现 ≥ 2 ATR 的反转，摆动点少于 4 个，按 feature-spec 字面被判为 `unavailable`（缺失）而不是 `no_range`（值 0）。T1 窗口里没有发生（mult=2 下最少 14 个 swing），但强趋势 regime 里会。已在 `scripts/test-structure.mjs` 中把当前字面行为写成断言并注释。

**已知局限（追加）**
- 头部币（BTC/ETH 等 anchor）不在 20 币样本中，其区间行为可能不同；P5 实现 `f1` 后须对 anchor 币单独检查
- 4h 版本沿用 `mult`/`tol`（ATR 相对量，尺度无关），`lookback` 取 84（4h × 84 = 14 天）；4h 的覆盖率待 P5 实测，低于 80% 需回头调整

### T3 · 三态语义裁定、预登记备选与 train/serve 一致性探针（2026-09-20）

**裁定（架构会话，写入 `docs/feature-spec-v1.md`）**：缺失与 0 的判据是 **"0 说出来的那句话，在这个状态下是不是真的"**（R6），不是"能不能算出来"。

| 情形 | f1 / f3 | f2 |
|---|---|---|
| 数据不足（bar < 168，或 ATR 无效/为 0） | 缺失 | 缺失 |
| 数据充足、确认摆动点 < 4 | **0**（`no_range`） | **缺失** |
| 数据充足、摆动点 ≥ 4 但无相干边界 | **0**（`no_range`） | 照算 |

f2 的 0 是"无明确趋势"，在强趋势里说的是反话，所以不对称。`f3 = 0` 是把观察到的状态编码在连续量表最"不像区间"的一端，不是填补缺失；代码注释已写明，禁止改成中位数填充。

**预登记的备选（防止将来钓参数）**：若 P3 中 `f1`/`f3` 的 ΔBSS 接近 0，第一个要检验的假设是"摆动尺度选错了"，预登记备选为 **`zigzagAtrMult = 1.5`、`rangeTolAtr = 0.5`**。该备选**只允许在验证折上试一次**，结果无论好坏都如实记录；**不允许扫描 mult × tol 网格去找最好的一格**。

**参数敏感性的记账**：主配置 (mult 2, tol 0.5) 处在斜坡而非平台上。has_range 覆盖率（cap 25、触碰 ≥ 2）：tol 0.3 / 0.5 / 1.0 下，mult=1.5 为 89.2 / 93.3 / 96.0%，mult=2 为 72.4 / 85.3 / 94.7%，mult=3 为 23.4 / 38.5 / 61.6%。参数已冻结不再调整；上线后真正的风险是**时间稳定性**（该窗口内按月 0.874 / 0.825 / 0.866）。

**train/serve 一致性探针**（特征侧，无标签）：ZigZag 是路径依赖的，而线上只有最近 360 根、训练可以喂多年历史。用 T1 的 20 币 × 2184 根，在每个评估点比较"全历史上的 `analyzeStructure`"与"仅最近 360 根上的结果"：

- 6100 个评估点：`rangeState` 一致 **100%**，窗口内确认摆动点数一致 **100%**
- 其中 5180 个 has_range 点：上沿、下沿、触碰数、包含率逐值一致 **100%**（1e-9）
- 额外历史长度 <200 / 200–600 / 600–1200 / ≥1200 根四档均为 100%

结论：在冻结参数（lookback 168、360 根窗口）下，ZigZag 在回看窗口开始之前已重新同步，训练与线上不会因窗口起点不同而分歧。此性质已固化为回归测试 `train/serve skew guard`（`scripts/test-structure.mjs`）。仅在 1h 上验证；4h（lookback 84）待 P5 单独验证。

**同日描述性快照观察（当前快照，仅供设计参考，未用于选参数）**：589 个永续合约中，**128 个（21.7%）的当前资金费率恰好等于 0.0001**（基准值），147 个在其 ±0.00002 内；结算周期分布为 4h × 434（73.7%）、8h × 131、1h × 4、无数据 × 20。这直接关系 `a3_funding_z`（见审计意见），待 P2 回填费率历史后再实测其历史分布。

### T4 · 特征注册表实现与 W1 上的特征侧可用性观察（2026-09-20）

**实现**：`lib/indicators/features/`（`registry.ts` 的 `buildFeatureVector` 是唯一实现；`context.ts` 的 `buildCrossSection`、`funding.ts`、`structureFeatures.ts`、`stats.ts`）。19 个特征，`FEATURE_VERSION = "features-v1"`，只读最近 400 根。测试 `scripts/test-features.mjs`（16 项，含 R1 机械守卫：决策点之后的数据、400 根窗口之前的历史都不能改变任何特征值）。

**`a3_funding_z` 尚未启用**：`FUNDING_SCALE_FLOOR = null`，函数对任何币都返回缺失并注明原因。待 W1 上的费率历史回填、按协议取"非零 IQR 的 p10"并报告 ±5 clip 率后再设值。

**W1 上 1h 自足的 9 个特征（描述性，未用标签）**：20 币 × 每 6 根取一点，从第 349 根起，共 6120 点。

| 特征 | 缺失率 | 为 0 的比例 | p10 / p50 / p90 |
|---|---|---|---|
| c1_effort_vs_result | 0.0% | 0.7% | −44.6 / −3.9 / 40.8 |
| c2_sell_absorbed | 0.0% | 9.2% | 0.014 / 0.32 / 0.48 |
| d1_vol_squeeze_pct | 0.0% | 0.0% | 7.3 / 50.4 / 92.4 |
| d2_volume_dryup_pct | 0.0% | 0.0% | 6.7 / 44.5 / 89.1 |
| d3_compression_bars | 0.0% | 50.6% | 0 / 0 / 47 |
| f1_sweep_reclaim | 0.0% | 41.9% | 0 / 0.052 / 0.63 |
| f2_up / f2_down | 0.0% | 72.7% / 66.2% | — |
| f3_range_containment | 0.0% | 15.1% | 0 / 0.48 / 0.84 |

**f1 的噪声底**：58.1% 的点 `f1 > 0`，`f1 > 0.5` 占 14.4%，`f1 > 1` 占 2.7%。原因：区间下沿取簇内摆动低点的**均值**，簇的成员自己的影线约一半会低于均值而收盘收回，于是普通震荡也持续触发一个很小的 f1。这是定义的性质，不是实现错误；是否改为以簇内最低点（真正的止损边缘）为界，交由架构裁定。

**其余尚无数据的特征**（4h、BTC、横截面、费率）的联合覆盖率仍未测，需要 P2 的采集与回填。

### T5 · 区间边界改为簇内极值后的重测（2026-09-20）

**变更**（架构裁定，见 `docs/feature-spec-v1.md`「区间边界取簇内极值」）：区间下沿 = 簇内**最低**的摆动低点，上沿 = 簇内**最高**的摆动高点（原为簇内均值）。原因：均值边界下成员自己的影线约一半必然低于均值，这是算术保证的，与价格是否被猎杀无关，所以 T4 里 58.1% 的点 `f1 > 0` 是伪装成信号的噪声。同步落地：4h 的 sweep 窗口 24 根（1h 仍为 48）。冻结参数（mult 2 / tol 0.5 / lookback 168 / touches ≥ 2 / cap 25）不变。

**数据与脚本**：仍是 W1 的本地 `klines-1h.json`（SHA256 `8b1bca60…fe87`，未重新下载）；`scripts/measure-range-params.mjs` 与新入库的 `scripts/measure-feature-availability.mjs`。无标签。

**区间检测（T2 对照，mult=2 / tol=0.5，6680 点）**

| 项 | 均值边界（T2） | 极值边界（本次） |
|---|---|---|
| has_range（cap 25、触碰 ≥ 2） | 85.3% | **85.7%**（≥ 80%，参数照旧） |
| no_range 原因：上沿簇不高于下沿簇 | 233 | 206 |
| `heightAtr` p10/p25/p50/p75/p90 | 1.48 / 2.17 / 3.24 / 5.09 / 8.26 | 2.07 / 2.77 / 3.82 / 5.56 / 8.60 |
| 包含率 p10/p25/p50/p75/p90 | 0.27 / 0.38 / 0.53 / 0.71 / 0.86 | 0.30 / 0.42 / 0.58 / 0.77 / 0.91 |
| has_range 按月（9/10/11） | 0.874 / 0.825 / 0.866 | 0.877 / 0.829 / 0.871 |
| 敏感性（tol 0.3/0.5/1.0）mult 1.5 | 89.2 / 93.3 / 96.0 | 89.8 / 94.1 / 97.0 |
| mult 2 | 72.4 / 85.3 / 94.7 | 72.6 / **85.7** / 95.5 |
| mult 3 | 23.4 / 38.5 / 61.6 | 23.5 / 38.6 / 61.9 |

边界变宽符合预期（`heightAtr` 与包含率整体上移，覆盖率与月度稳定性基本不变）。

**f1 分布（1h，6120 点）——本次改动是否奏效的判据**

| 指标 | 均值边界（T4） | 极值边界（本次） |
|---|---|---|
| `f1 > 0` 占比 | **58.1%** | **40.6%** |
| `f1` 为 0 的比例 | 41.9% | 59.4% |
| `f1` 中位数 | 0.052 | 0 |
| p75 / p90 / p99 | — | 0.286 / 0.612 / 1.307 |
| `f1 > 0.1` | — | 36.3% |
| `f1 > 0.5` | 14.4% | 14.0% |
| `f1 > 1` | 2.7% | 2.6% |

判据（架构预设）："若仍 > 50% 说明根因没抓准"。40.6% < 50%，**判据通过**。如实的解读：被清掉的是均值边界造成的那部分（中位数 0.052 → 0；`0 < f1 ≤ 0.1` 只剩 4.3%），但**上尾几乎没变**（`f1 > 0.5` 14.4% → 14.0%）。剩下的非零值中位数约 0.35 ATR，不是近零噪声，而是"最近 48 根内有一根 K 线的低点跌破了该簇全部已知摆动低点并当根收回"——这类小幅破位后收回在 1h 加密数据上本身就常见。f1 是否有预测力仍是 P3 的问题，本次只证明消除了构造性噪声：干净震荡序列上 `f1` 恒为 0（测试 `f1 boundary is the cluster extreme`）。

**未改变的**：`FUNDING_SCALE_FLOOR` 仍未测（需要费率历史，币安 API 本机不可达，见下）。

### T6 · 币安连通性诊断（2026-09-20，非数据实验，记录以免重复误判）

- 现象：`fapi.binance.com` / `dapi.binance.com` / `api.binance.com` 从本机直连均超时；`data.binance.vision` 正常。
- 系统 DNS 对三个 API 主机每次返回不同的非币安地址（应答注入）；经 DoH 拿到的 CloudFront 真地址上 **TCP 能连（40–211ms），但以 `fapi/dapi.binance.com` 为 SNI 的 TLS 握手被 `ECONNRESET`**。架构会话另做的对照：**同一 IP 换 SNI 为 `data.binance.vision` 则握手成功**。结论：过滤发生在 SNI 层，**修 DNS 无效**。
- 教训：只测到 TCP 连通不能证明应用层可用；网络诊断先分层（DNS → TCP → TLS → HTTP）。
- 交付：`scripts/binance-net.mjs`（`preflight` 显式报告路径与失败层次，握手被重置时抛错且不提供绕过；`createGet` 保留域名只替换地址解析；不使用 `HTTPS_PROXY`）、`scripts/check-binance-net.mjs`、`scripts/test-binance-net.mjs`。
- **未采取**任何规避手段（换 SNI、域前置、ECH、代理）：那已不是修 DNS，而是绕过网络层过滤，须由用户决定。
- 影响：币安实时接口的采集/回填与 `FUNDING_SCALE_FLOOR` 的测量挂起；`data.binance.vision` 的长期使用仍待用户授权。

### T7 · 标签 k 的分布测量（2026-09-20）

**目的**：三重障碍标签的 `k`（障碍宽度，单位 ATR%）待测量；规则：三类里任何一类 < 15% 或 > 60% 则 `k` 要调。W1 是已消耗窗口，在其上只看标签分布、不训练任何模型，不影响测试折。

**方法**：`scripts/measure-label-distribution.mjs`；W1 的 20 币，每 6 根取一点。障碍 = 入场价 × (1 ± (k·ATR% + cost))，成本 = 2×0.05% + 点差 + 滑点。**点差用各币"当前快照"的点差代替整个窗口**（没有历史点差，已知近似）；**滑点尚无约定估计**，只作敏感性（0 与 0.05%），不是选择。同一根 bar 内两个障碍都被触及 ⇒ 判"down"（默认，架构裁定），并统计其占比。ATR 用 1h ATR14。

**4h 头（h=4，滑点 0.05%，n=7220）**

| k | down | flat | up | 同 bar 双触 | 判定 |
|---|---|---|---|---|---|
| 0.5 | 52.4% | 7.0% | 40.7% | 4.4% | flat < 15% |
| **1.0** | 33.1% | 38.4% | 28.5% | 0.4% | **ok** |
| 1.5 | 19.6% | 62.7% | 17.6% | 0.1% | flat > 60% |
| 2.0 | 11.7% | 77.4% | 10.9% | 0.1% | 三类均越界 |

**24h 头（h=24，滑点 0.05%，n=7160）**

| k | down | flat | up | 同 bar 双触 | 判定 |
|---|---|---|---|---|---|
| 1.0 | 52.8% | **1.3%** | 45.9% | 0.5% | flat < 15% |
| 2.0 | 43.3% | 16.4% | 40.3% | 0.1% | ok |
| **2.5** | 36.5% | 28.5% | 35.0% | 0.1% | **ok** |
| 3.0 | 30.3% | 41.0% | 28.7% | 0.1% | ok |
| 4.0 | 19.5% | 61.2% | 19.3% | 0.0% | flat > 60% |

滑点取 0 时各格数字差 ≤ 2 个百分点，判定不变。

**发现**
1. **单一的 k 不能同时服务两个头。** k = 1.0 对 4h 健康，对 24h 的 flat 只有 1.3%（障碍按 **1h** ATR 设，24 根 bar 的走势远超 1 个 1h ATR）。24h 的可接受区间约为 k ∈ [2, 3]；随机游走的 √ 标度给出 k_24 = 1.0 × √(24/4) ≈ 2.45，落在区间内。**未选定**，待架构裁定。
2. 在合格的 k 下，同一 bar 内双触占比只有 0.1%–0.4%，所以"判 down"的不对称性在这个窗口里几乎不起作用；只有 k ≤ 0.5（且不合格）时才到 4%–5%。
3. down 比 up 略多（4h k=1：33.1% vs 28.5%；24h k=2.5：36.5% vs 35.0%）。这是 W1（2025-09 至 11 月）这个单一 regime 的漂移，不能推广；同 bar 政策的贡献可忽略。

**已知局限**：点差为当前值的代用品；滑点无估计；20 币、单一 91 天窗口。

### T8 · k 的 √ 标度冻结、成本假设、阈值分类与 isotonic 预登记（2026-09-20）

**k 的裁定**（架构）：`k_h = 1.0 × √(h/4)`，即 4h ⇒ 1.0，24h ⇒ √6 ≈ 2.449。理由是自由度：独立设两个 k 是两个参数、两次钓鱼机会；√ 标度是一个参数加一条规则，将来加窗口也不必重测。**`LABEL_K_BASE = 1.0` 是"测过的合格值"，不是最优值**：4h 只测了 0.5（flat 7%，不合格）、1.0（合格）、1.5（flat 62.7%，不合格），没有细扫；我们找的是合格且不钓鱼的值，不是最优。

**验收：实际跑了 k = √6，不是插值**（`scripts/measure-label-distribution.mjs`，W1 的 20 币，每 6 根一点；滑点为往返值）

| 头 | k | 往返滑点 | n | down | flat | up | 同 bar 双触 | 判定 |
|---|---|---|---|---|---|---|---|---|
| 4h | 1.000 | 0.04% | 7220 | 33.3% | 38.1% | 28.6% | 0.4% | ok |
| 4h | 1.000 | **0.10%（假设值）** | 7220 | 32.0% | 40.4% | 27.6% | 0.4% | ok |
| 4h | 1.000 | 0.20% | 7220 | 29.9% | 44.1% | 26.0% | 0.3% | ok |
| 24h | 2.449 | 0.04% | 7160 | 37.1% | 27.1% | 35.9% | 0.1% | ok |
| 24h | 2.449 | **0.10%（假设值）** | 7160 | 36.7% | 28.1% | 35.3% | 0.1% | ok |
| 24h | 2.449 | 0.20% | 7160 | 36.0% | 29.7% | 34.3% | 0.1% | ok |

六格三类都在 [15%, 60%] 内，冻结：`LABEL_K_CALIBRATED = true`，`labelK(h)` 是唯一入口。√ 标度落在 T7 实测合格区间 [2, 3] 的中段（24h 上 k=2.5 时 flat 28.5%，与 √6 的 28.1% 一致），这是一个弱但真实的验证：它没有被强行凑进去。

**成本假设**（架构裁定）：往返成本 = 2 × 0.05%（手续费）+ 点差 + 滑点，滑点取**每边 0.05%（往返 0.10%）**，总计 **0.20% + 点差**。没有成交数据也没有历史盘口，无可测之物，所以规则是"必须猜的时候，往对自己不利的方向猜"。
- 偏向（**方向相反，不声称精确抵消**）：滑点假设 **偏悲观**；点差用各币"当前"点差代替整个历史窗口，**偏乐观**（历史流动性通常更差、点差更宽，真实成本被低估）。接受该代用品的理由：固定常数会丢掉币种间的流动性差异，而那是主要的变差来源。
- **预先登记的敏感性检查**：滑点每边 0.02% / 0.05% / 0.10%（往返 0.04% / 0.10% / 0.20%），报告标签分布（上表已做）与 P3 之后的 BSS。目的是知道结论对这个猜测有多敏感，而不是假装它精确。已知 k 的判定对滑点不敏感（三档均合格）。

**同 bar 双触判 down**：合格 k 下只占 0.1%–0.4%，保留。W1 里 down 比 up 略多是单一 regime 的漂移，不要推广。

**阈值的两类**（`lib/calibration/gate.ts`）

| 类别 | 例子 | 规则 |
|---|---|---|
| 性能阈值（多好才算好） | BSS 门槛 | **绝不能拍**：对着实测的随机基线 p95 比，或只比符号（> 0） |
| 充分性阈值（证据够不够） | 折数、样本数、跨度 | 可以是判断，**但必须标明是判断**；罪不在使用判断值，而在把判断值伪装成测量值 |

- `minEffectiveN` 改为**派生量**：20 × 自由参数数，自由参数数 = (类别数 − 1) × (特征数 + 1) = 2 × 22 = 44 ⇒ **880**（随特征集变化自动调整；30 个特征则为 1240）。"每参数 20 个有效样本"是有出处的经验法则，不是拍的数。取代此前从交接文档沿用的 2000。
- **测试折有效样本量单列**：`minEffectiveTestN = 50（每箱）× 5（箱数）= 250`，绑定 ECE 的分箱要求；每类样本数不得为 0，类别下限本身仍未设置。
- `minFolds = 3`、`minTestSpanDays = 60` 保留，代码与文档里标为 **"判断值，非测量值"**。
- **fail-closed 漏洞修复**：阈值对象若缺字段（或为 NaN、字符串），`x < undefined` 为 false，检查会被静默跳过而通过。现在所有阈值必须是有限数，否则失败（`threshold … not configured`），并有逐字段测试。

**isotonic 校准器：暂缓，预先登记分支**（架构裁定）：还不知道模型有没有任何 skill，先建校准层是本末倒置，且它需要单独的校准折，会让 walk-forward 复杂化。
- 若门槛**仅因 ECE 不过**而 BSS 与残差 BSS 都通过 ⇒ 是正则化收缩造成的校准偏移，此时才上 Platt / isotonic，且**只在验证折上拟合，绝不用测试折**。
- 若 BSS 本身 ≈ 0 ⇒ 校准无意义，不做。

### T9 · 币安连通性恢复、资金费率回填与 FUNDING_SCALE_FLOOR 测量（2026-09-20）

**连通性**：用户把 VPN 出口切到日本后（系统代理模式，Node 不会自动走），经本机代理访问币安返回 200；此前的 451 是因为出口在美国。直连（DNS 污染 + SNI 过滤）仍不可用。`scripts/binance-net.mjs` 现在按 直连 → `HTTPS_PROXY`（显式读取环境变量）→ DoH 的顺序探测并**明确打出所用路径**；服务器不设该变量则自动直连，代理不进产品。**硬边界写进代码**：任何路径上遇到 **451 立即停止**（`RegionBlockedError`），不得更换节点重试；限速（418/429）与网页防火墙（403）遇到就停，不绕。实现中修掉一个自己引入的 bug：`agent: false` 时 Node 忽略自定义 `createConnection`，会悄悄连回系统解析出的假地址，改用 `https.Agent` 子类。

**回填**：`scripts/backfill-funding.mjs`（可续跑、分页、遇限制就停）拉 W1 前后（2025-08-02 至 2025-12-01，含 30 天前导）的 `/fapi/v1/fundingRate`，写入 git 忽略的 `data/calibration/funding/`。人群：UM · USDT · 永续 · 上市早于 2025-07-03（359 个）。每币约 726 行（4h 周期），一个请求。
- **两次 HTTP 403**（币安网页防火墙的 HTML 页，不是 JSON 错误）：第一次在约 126 个币之后（800 ms 间隔），停下后 200 恢复，节奏放慢到 1500 ms 续跑一次；**第二次在 308 个币处**。按事先约定**彻底停下，不再续跑，也不再向币安发任何请求**。
- **样本**：308 / 359 个币（86%），按字母序为 `1000000BOB…` 至 `SYN…`，**缺 SYN 之后的 51 个**。不是随机抽样，但费率 IQR 与币名无关，影响预计有限；仍须声明。

**测量**（`scripts/measure-funding-floor.mjs`；W1 内每 6 小时一个评估点，308 币，112,112 点，其中 112,111 有效；无标签）
- 尾随 30 天 fundingDaily 的 **IQR = 0 占有效点的 36.85%**（费率整整 30 天是常数）。非零 IQR（%/天）p1/p5/p10/p25/p50/p75/p90/p99 = 0.000621 / 0.00454 / **0.009252** / 0.01985 / 0.04322 / 0.1091 / 0.2084 / 1.001。
- **协议下限 = 非零 IQR 的 p10 = 0.009252 %/天。此时 ±5 的 clip 率 = 5.82% > 2% ⇒ 协议判定 FAIL（"下限取低了，回去找架构师"）。未调整，`FUNDING_SCALE_FLOOR` 仍为 null，`a3` 仍停用。**
- 分层：IQR = 0 的点 clip 7.69%，IQR > 0 的点 4.74%；按最新一行的结算周期：1h 9.23%（n = 1539）、4h 6.73%（n = 70623）、8h 4.09%（n = 39949）。
- 敏感性（仅记录，不是搜索）：下限取非零 IQR 的 p5 / p10 / p25 / p50 时 clip 率为 7.65% / 5.82% / 3.85% / **2.21%**。

**对判据本身的发现**（供架构裁定，未采取行动）：把 clip 率拆成"下限可控"和"自然厚尾"两部分（下限 = p10）——
| 部分 | 占点数 | clip 率 | 占全部 clip |
|---|---|---|---|
| IQR < 下限（IQR = 0 及极小 IQR） | 43.16% | 8.31% | 61.62% |
| IQR ≥ 下限（自然尾部） | 56.84% | 3.93% | 38.38% |

自然尾部按 IQR 分档的 clip 率：0.009–0.02 → 7.76%；0.02–0.05 → 4.03%；0.05–0.1 → 3.04%；0.1–0.3 → 2.31%；**≥ 0.3 → 2.08%**。即使在下限完全不起作用的最大 IQR 段，clip 率也约 2%：资金费率是厚尾的，"偏离超过 5 倍 IQR"天然占 2%–4%。所以"clip 率 ≤ 2%"这个判据**无法靠抬高下限达到**（下限抬到 p50 才 2.21%，而那时对一半以上的点 z 已退化为绝对偏离而不是相对自身波动的偏离）。另：IQR = 0 的点里有 17.45% 的当前费率偏离了那个常数（真实的跳变），跳变幅度中位数 0.0388、p90 0.216、p99 1.33（%/天），相对下限 0.00925 分别是 4.2 倍 / 23 倍 / 144 倍。

**未做**：K 线分页与归档数据的对账（回填脚本 `scripts/backfill-klines.mjs` 与对账脚本已写好并有单测，但 403 之后不再向币安发请求，待架构与用户裁定后再跑）。

### T10 · FUNDING_SCALE_FLOOR 冻结、a3 启用与 403 之后的采集策略（2026-09-20）

**判据作废**（架构裁定，承认错误）：T9 里"±5 的 clip 率 ≤ 2%"是一个**凭空的数字**，且它把两个互不相关的机制压成了一个指标——下限（防止 IQR≈0 时除零，只对缺乏真实尺度的点起作用）与 ±5 clip（缩尾，防极值主导线性模型）。T9 的分解证明它**不可达**：即使在下限完全不起作用的最大 IQR 段（≥ 0.3），clip 率也仍有 2.08%——那是资金费率厚尾自身的水平；要压到 2% 以下只能把下限抬到中位数以上，那时一半以上的点 z 退化成绝对偏离，直接违反 R2。**作废。**

**改成语义判据**：下限正确 ⟺ 它只对缺乏真实尺度的点起作用，且这些点产出的 z 语义正确。T9 的数据已完成该验证：
- IQR ≥ 下限（56.84% 的点）⇒ 下限按定义无影响；
- IQR = 0 且当前费率等于该常数（IQR=0 点的 82.55%）⇒ z = 0，真值是"费率一直没动，现在也没动"；
- IQR = 0 且当前费率跳变（17.45%）⇒ z 很大、clip 到 ±5，真值是"一个费率从不波动的币，今天动了"，本来就该是极端值。
三种情形语义全对，**下限通过**。

**冻结**：`FUNDING_SCALE_FLOOR = 0.009252`（%/天，W1 非零 IQR 的 p10），clip 边界 ±5。**clip 率 5.82% 记为诊断量，不作门槛。** **`a3_funding_z` 启用**（代码 `lib/indicators/features/funding.ts`）。**预先登记**：若 P3 中 `a3` 的 ΔBSS 接近 0，备选假设是"±5 对厚尾饱和过度"，备选值 ±10，**只在验证折上试一次**，结果无论好坏都记录。

**a3 在 W1 上的覆盖与分布**（`scripts/measure-a3-coverage.mjs`，真实的 `fundingZ` + 冻结下限，无标签、无网络；308 币，112,112 点）
- **覆盖率 99.999%**（112,111 点可算；1 点因"最新一次结算过旧"缺失）；逐币最低 99.73%，只有 1 个币不到 100%。
- a3 的 p1/p5/p25/p50/p75/p95/p99 = −5.000 / −5.000 / −0.478 / 0.000 / 0.000 / 0.565 / 3.488。**精确为 0 的占 46.94%**（零膨胀，是结构事实：费率常数且没动）。
- 触及 ±5：**+5 占 0.79%，−5 占 5.03%**，合计 5.82%。**明显不对称，几乎全在负侧**：W1（2025-09 至 11 月）是单一的偏空 regime，费率下行、跌回基准以下或转负时，对"一直恒定在基准值"的币会饱和到 −5。这是这个窗口的特征，不能推广，也提示 a3 的分布随 regime 变化（校准折需覆盖多个 regime）。
- 按最新结算的周期：1h（n=1539）clip 9.23%；4h（n=70623）6.73%；8h（n=39949）4.09%。

**缺失的 51 个币**：W1 的费率回填停在 308/359（字母序 `SYN…` 之后 51 个未取）。"IQR 与币名无关"是论证不是测量——**采集恢复后补齐**并复核上述数字。

**403 之后的采集策略**（架构裁定）
- 两次 HTTP 403 均为币安网页防火墙的 HTML 页；1.25 请求/秒远低于官方限额，两次分别发生在累计约 126 与 308 个请求之后，判断为共享出口 IP 上的 WAF 启发式，**不是调参能解决的问题，继续试就是在摸阈值**。**不恢复批量采集。**
- 允许的动作：等待 **≥ 30 分钟**后发 **1–2 个**请求确认 IP 状态，**仅用于向用户报告，不得据此重启采集**；若再次 403，连探测也停。
- `data.binance.vision`：WAF 问题使其价值上升（归档源根本不打 API，而多年回填正是请求量最大、最易触发 WAF 的部分）。**授权仍是用户的决定，保持暂缓，不下载。**
- K 线深回填默认 `limit = 500`（权重 2，每根 0.004，比 1500 的 0.0067 省）。**未知**：WAF 是按请求数还是按流量触发。若恢复采集后 WAF 再次出现，**第一个要试的变量是改用 `--limit 1500`（请求数更少）**，无论结果都记录。

### T11 · regime 覆盖门槛（2026-09-20，设计已定，切点待数据）

**动机**：a3 在 W1 只在 −5 侧饱和（5.03%）而几乎不在 +5 侧（0.79%），不是分布平移，而是**饱和模式本身随 regime 变化**：牛市里费率抬升，饱和会跑到 +5 侧，被截断的是不同的尾巴。只在单一行情里验证过的模型输出自信的概率，是"假精度"的典型形态。`minTestSpanDays` 只是日历代理，看不见这件事；`minRegimes` 只在 handoff 草案里，代码里从来没有。

**设计**（架构裁定；代码 `lib/calibration/regime.ts` 与 `gate.ts`）
- **只用特征侧数据 g1（市场宽度）与 g2（BTC 波动分位），训练前冻结，不看标签**；模型看不到 regime 标签，所以不是选模型也不是标签泄漏。
- **两条边际要求，不做 g1 × g2 网格**：网格 9 个格子填不满，且 g1 与 g2 相关。分别要求测试折**同时含 g1 最高与最低三分位、g2 最高与最低三分位**，四个档位**各自** ≥ `minRegimeBinSamples` 个**有效**（唯一度加权）样本。两个轴都要，因为失效模式不同：g1 管方向性 regime（a3 的饱和侧），g2 管波动 regime（ATR 类特征）。
- **`minRegimeBinSamples = 100` 是判断值，非测量值**（注释已标）。
- **切点**：g1、g2 在**全部可用历史**上的三分位，**算一次，冻结为仓库里的常数，记入台账，每次运行不得重新推导**。这只是"高波动"这个词的定义；每轮重算会让门槛随数据漂移，那就什么也没门住。三分位边界是包含的（值 ≤ lower 为低档，值 ≥ upper 为高档）；非有限值不计入任何档。
- **fail-closed**：`regimeCutpoints` 默认 null，null ⇒ "regime coverage unverified: regime cutpoints not configured" ⇒ 不通过；报告没有覆盖、某个档位缺失/NaN/为 null/不足、只覆盖一个轴、覆盖是用**不同切点**算出来的、`minRegimeBinSamples` 未配置——一律失败，原因逐条列出。**没有"记录警告但放行"的口子。**
- 边界测试：null；四个档位分别为 0 / 99.9 / 恰好 100 / NaN / null；只覆盖 g1 的一档；某个轴整体缺失；某个轴很多、另一个轴很少；切点不一致、切点无效（lower ≥ upper、NaN、只有一个轴）；`minRegimeBinSamples` 为 NaN/null/undefined/0/字符串。

**后果（预期内，不是障碍）**：**在拿到跨 regime 的历史数据之前，P3 无法认证任何模型，面板不会显示概率。** W1 只有一个偏空 regime，无法用它来定切点，所以切点的测量本身要等多年历史。这给"需要多年历史"提供了具体理由：不是"样本更大"，而是"没有跨行情的数据就不能上线"。

**未做**：g1/g2 三分位的测量（需要多年历史，等数据通道）。

### T12 · 联合覆盖率试点、400 根窗口回归与新币的解析推算（2026-09-21）

**下载（用户在本会话逐批批准，2026-09-21）**：来源 `data.binance.vision/data/futures/um/monthly/`，每个文件都与其 `.CHECKSUM` 的 SHA256 比对通过，无缺失、无校验失败。共约 2.4MB：20 个 W1 币的 4h（2025-06 至 11，120 个 zip，1.09MB）、20 个 W1 币的 1h（2025-08，20 个 zip，0.67MB）、BTCUSDT 的 1h（2024-08 至 2025-11，16 个 zip，0.61MB），以及**1 个** fundingRate 探测文件（ARBUSDT 2025-09）。脚本 `scripts/fetch-archive.mjs`（只做参数所说的事，从不自己决定下载什么；4 项单测）。**归档源提供 fundingRate 月度文件**；该探测文件的 90 行与此前 API 回填的同月数据 **90/90 逐行一致**，且归档还直接给出结算周期列。补齐缺失的 51 个币的费率历史因此可以走归档而不打 API，等用户再批准那一批。未下载任何新币子样本（见下"新币"）。

**联合覆盖率（W1，20 币，每 6 根一个决策点，7280 点；`scripts/measure-joint-coverage.mjs`，真实的 `buildFeatureVector`，无标签、无网络）**
- 无费率历史的币（ZKUSDT，本地回填缺口）排除，剩 19 币 / 6916 点。
- **21 个特征里 18 个是实测的：联合覆盖率 99.7%。** 缺失全部来自 `f2_up/f2_down`（"确认摆动点 < 4"，各 11 点）与 `f2_up_4h/f2_down_4h`（各 9 点）：这正是 R6 里"f2 缺失、f1/f3 为 0"那条不对称规则在真实数据上的样子；其余 14 个特征缺失率均为 0.0%。逐币最低 97.3%（CELRUSDT）。
- **e2 / e3 / g1 不是实测的**：它们要求同时点 ≥50 个有效币，20 币样本满足不了（100% 因"横截面太小"而挂起）。线上有约 500 个币时这条必然满足，但"必然可算"是**推理**，不是测量；上面的 99.7% 是不含这三项的实测值，含"假定这三项可算"的数字也是 99.7%（这三项自身的其余前置条件——连续 15 根、ATR 有效、24h 收益可算——在样本里没有一个点失败）。
- 老币 / 新币：按上市 ≥ 180 天与 < 180 天分：老币 99.7%（n = 6076）、新币 100.0%（n = 840）。**但这个样本里最年轻的币也有 76 天**，测不到真正的新币；覆盖率只是**上界**，不是对全体币的估计。

**新币不需要再下载来测，可以从窗口长度直接推算**（当前快照：526 个 UM USDT 永续；上市年龄 p5 / p10 / p25 / p50 = 178 / 261 / 383 / 634 天）
| 窗口要求 | 最小上市天数 | 满足的币 |
|---|---|---|
| 1h 结构窗口（168+14+1 根）f1/f2/f3 | 7.6 | 526/526 |
| e1 beta（169 根 1h） | 7.0 | 526/526 |
| 1h 尾随 336 + ATR 预热（349 根）c1/d1/d2/d3 | 14.5 | 524/526 |
| 4h 结构窗口（84+14+1 根）f1_4h/f2_4h | 16.5 | 524/526 |
| a3 费率窗口（跨度 ≥ 25 天、≥ 61 行） | 25.0 | 522/526 |
| **4h 尾随 336 + ATR 预热（349 根 4h）d4** | **58.2** | **520/526** |
**决定性的门槛是 d4（58.2 天）**：比它更年轻的币按 R3 必然没有概率（确定性的，不是估计），当前只有 **6 / 526（1.1%）**，其中 2 个连 1h 特征都不满足。综合：全体币里"年龄足够"的占 98.9%，再乘以样本上实测的 99.7% ⇒ 线上联合覆盖率的**上界约 98.6%**。它仍是上界：不含采集失败（缺根、API 错误）造成的缺失，那要等 P2 的实际采集才能测。
**含义**：架构师担心的"新币会把覆盖率拖到门槛以下"在当前币种池里不成立（新币只占 1.1%）；但也要看到设计上的取舍：**该系统按构造不会对上市不足 58 天的币出概率**，而这个门槛几乎全部来自 d4（其余特征只要求 ≤ 25 天）。若产品目标包含"新上市的币"，d4 是要重新审视的那一个；这是架构问题，不是实现问题。

**400 根窗口回归（`scripts/verify-structure-window.mjs`；对结构特征比较"全历史上的分析"与"仅最近 400 根上的分析"）**
- 1h：8440 点，rangeState 与摆动点数 **100.00%** 一致；7308 个 range 点的边界/触碰/包含率 **100.00%** 一致（此前只在 360 根上验过）。
- 4h（lookback 84）：2324 点，rangeState 与摆动点数 **99.91%** 一致（2 点不同）；1239 个 range 点的边界/触碰/包含率 **100.00%** 一致。4h 可用的额外历史更短（共 1098 根），且窗口只有 4.7 个 lookback，ZigZag 的起点效应比 1h 略明显，但很小。
- 这**不影响** train/serve 一致性：`buildFeatureVector` 在训练和线上都只读最近 400 根（R1 机械保证），两边看到的输入本来就相同；本探针衡量的是"400 根窗口的结构结果对起点是否敏感"。

**局限**：20 币、单一 91 天窗口、单一（偏空）regime；e2/e3/g1 未实测；不含采集失败。

### T13 · 年龄排除的可读化、周期推断的 oracle 校验与联合覆盖率的更正（2026-09-21）

**联合覆盖率的分析更正（架构会话）**：早先"0.95^16 ≈ 44%"的担心是错的。它假设各特征的缺失**相互独立**所以会连乘；实际上缺失几乎完全相关，绝大多数由同一个条件驱动（bar 数够不够）。**缺失高度相关时，联合覆盖率 ≈ 最小的单项覆盖率，而不是连乘。** T12 的 99.7%（18 个实测特征）就是这个现象：剩下的 0.3% 全部来自 f2 的"确认摆动点 < 4"，R6 那条不对称规则在真实数据上只触发在它该触发的地方。98.6% 仍是**上界**：不含采集失败（缺根、API 错误、合约下架），那要等 P2 的真实采集才测得到。

**d4 的 58 天边界：保持不变**（架构裁定）。为 1.1% 的币去改一个已冻结的定义，代价与收益不成比例。但要让边界不再隐形：
- 缺失原因具体化：不再是"fewer than 349 bars"，而是 `history too short: needs 58.2 days (349 bars), has 12.3 days (74 bars)`（1h、4h、e1 的 beta 窗口、结构窗口同理；"缺根"与"历史太短"分开报告）。
- `summariseCoverage(vectors)`：每轮汇总有多少币完整、多少币被年龄排除、按特征的缺失计数；采集日志每轮都应打印。
- **预先登记的重议触发线**：被年龄排除的比例 **> 5%**（严格大于，恰好 5% 不触发），或产品目标明确纳入"新上市币"；届时 d4 的 4h 尾随窗口是唯一该重新审视的东西，不是之前。常量 `AGE_EXCLUSION_REVIEW_SHARE = 0.05`。

**资金费率结算周期推断的 oracle 校验**（`checkIntervalInference`，`scripts/check-funding-interval-oracle.mjs`；无网络）
- 归档的 fundingRate 文件带**真实的** `funding_interval_hours` 列，API 不返回它。**生产路径必须继续推断（R1）**：训练用归档真值、线上用推断，就是教科书式的 train/serve skew。归档列**只作校验真值**，不作特征来源。
- 现有的唯一真值文件（ARBUSDT 2025-09，90 行）：可比较 89 行，一致 89，不一致 0，拒绝 0。**证据很弱**：常数 8h，没有周期变化，没有漏结算。
- **周期变化在真实数据里并不罕见**（本地 API 回填数据，308 币，含前导 30 天）：**21 个币发生过周期变化**，共 30 次：4h→1h 21 次、1h→4h 7 次、8h→1h 2 次；按月 8 月 3、9 月 5、10 月 6、11 月 16。另有 1 个被推断拒绝的行：API3USDT 2025-09-21 08:00，与上一行相隔 2.99999 小时——1h→4h 切换时第一个 4h 周期只有 3 小时，不在 {1,2,4,8}h 内，推断按"偏离超过 10% 则丢弃、不猜"丢掉它。这是设计意图内的行为，但只有归档真值才能说明那一行的真实周期是多少、丢掉它是否合适。
- 测试（`test-features.mjs`）：常数 8h 一致；中途 8h→4h→8h（每行周期 = 与上一行的间隔）一致；漏结算被拒绝而不算不一致；归档列在切换边界若滞后一行则被检出。
- **有检验力的最小批次（仅提议，未下载，须用户批准）**：上面 21 个换过周期的币里取约 10 个、切换所在月份的 fundingRate 月度文件（例如 API3USDT 2025-08/09、FUSDT 2025-10/11、DOODUSDT 2025-10、BIOUSDT 2025-10、KAVAUSDT 2025-11、LAUSDT 2025-11、LAYERUSDT 2025-11、LSKUSDT 2025-11、GPSUSDT 2025-11、MERLUSDT 2025-11），约 13 个小文件。

### T14 · 51 币归档费率补齐、FUNDING_SCALE_FLOOR 复核与周期 oracle 的预登记落地（2026-09-21）

**下载（用户在本会话逐批批准，2026-09-21）**：`data.binance.vision/data/futures/um/monthly/fundingRate/`，每个文件都与其 `.CHECKSUM` 的 SHA256 比对通过，无缺失。
- **批次 1**：51 个币（字母序 `SYRUPUSDT` 至 `ZRXUSDT`，含 XRP、TRX、XLM、XMR、TAO、WLD、TRUMP 等大市值币，也是 T9 因两次 403 而缺的部分）× 2025-08…11 共 204 个 zip，0.24MB。与 API 回填的同月数据在 ARBUSDT 上曾 90/90 一致。转成与 API 回填同格式的 `data/calibration/funding/<SYMBOL>.json`，**有意丢弃归档的周期列**（`scripts/import-archive-funding.mjs`）：真值列永不进入特征侧。至此 W1 的费率覆盖 **359/359** 个币（原 308）。
- **批次 2**：12 个 oracle 文件（API3USDT 2025-08、09；FUSDT 2025-11；MYXUSDT 2025-08、09、10；REDUSDT 2025-09；SUPERUSDT 2025-11；KAVAUSDT 2025-11；SKLUSDT 2025-11；BIOUSDT 2025-10；DOODUSDT 2025-10），围绕"现象"而不是围绕那一个异常点选取：覆盖唯一被规则 A 拒绝的行（API3USDT 2025-09-21 08:00）、全部 7 次 1h→4h 切换、两次 8h→1h 与若干 4h→1h。**已下载，未查看内容**（见下"预登记落地"）。

**FUNDING_SCALE_FLOOR 复核（方法不变，样本 308 → 359 个币，W1，130,676 个评估点）**（`scripts/measure-funding-floor.mjs`；无标签）
| 项 | 308 币（T9） | 359 币（本次） |
|---|---|---|
| IQR = 0 的点 | 36.85% | 36.88% |
| 非零 IQR p10（协议下限，%/天） | 0.009252 | **0.009446** |
| 非零 IQR p1 / p5 / p25 / p50 / p75 / p90 / p99 | 0.000621 / 0.00454 / 0.01985 / 0.04322 / 0.1091 / 0.2084 / 1.001 | 0.000614 / 0.004506 / 0.02021 / 0.04227 / 0.1039 / 0.1955 / 0.8991 |
| ±5 clip 率（诊断量，非门槛） | 5.82% | 5.67% |
| a3 覆盖率（用代码里的冻结下限 0.009252） | 100.00%（1 点因结算过旧缺失） | 100.00%（同 1 点） |
| a3 精确为 0 / 触及 +5 / 触及 −5 | 46.94% / 0.79% / 5.03% | 46.63% / 0.76% / 4.96% |
补上 51 个含大市值币后，p10 只移动了 **+2.09%**，整体分布几乎不变；旧的"2% clip 率"判据已作废，脚本里残留的 FAIL 字样已改为诊断输出。"明显移动"没有预先定义的量化标准，我不擅自发明：常量是否更新为 0.009446 交架构会话裁定（更新的影响：下限抬高 2%，只作用于 IQR<下限的点，即 43% 的点里的一小部分）。**冻结的是方法，不是数字在不完整样本上的取值。**

**周期推断 oracle：预登记已落到代码，先于任何真值文件被打开**（`lib/indicators/features/funding.ts`；规格 a3 (iv) 与「判读表的三处修订」）
- 规则 A（现行，生产）：吸附到 {1,2,4,8}h，偏离 >10% 丢弃。规则 B（候选，未用于任何特征）：按实际 delta 归一，超出窗口 delta 中位数 [0.5×,2×] 丢弃。
- **两个问题分开，证据门槛不同**：(1) 真值列的语义——所有"切换事件"（相邻两行都未被拒绝且推断周期发生变化），看最后一个旧行与第一个新行上的列值 `(旧,新)`=**period_ending_at_row**、`(旧,旧)`=**lag_old_period**、`(新,新)`=**lead_new_period**、其余=**other**；**≥ 10 个事件且全部同一模式**才算判明，少于 10 个是"untested"（不是通过），混合是"unresolved"（escalate）。(2) 规则 A→B——被拒绝的行按 `classifyRefusedRow`（真值 = 实际经过时间 ⇒ adopt B；= 名义新周期 ⇒ keep A；其余 ⇒ escalate），**≥ 3 个被拒绝行且语义已判明**才允许改规则，否则保留 A 并标"提示性"。
- **非拒绝行出现任何不一致 ⇒ escalate**，且**允许学到语义约定、禁止为提高一致率而调参**（生产拿不到真值列）；"与旧周期相等"的系统性滞后形态在不一致里**单独标注**（`equalsPreviousInferred`）。
- **首次 oracle（ARBUSDT 2025-09，89/89 一致）记为"未检验"，不是"通过"**：它是一整月的常数 8h，没有周期变化、没有漏结算，恰好避开了推断最容易出错的全部场合。
- **读取纪律**：`decideIntervalRule` 一次性作用于整批；只读一次，结果指向哪边都如实记录；数据模糊时不得倒向"更优雅"的规则 B。**校验尚未运行**。

### T15 · FUNDING_SCALE_FLOOR 更新为 0.009446；结算周期 oracle 的唯一一次决策读取（2026-09-21）

**FUNDING_SCALE_FLOOR = 0.009446**（架构裁定，取代 0.009252）：0.009252 是该方法在 308/359 个币上的输出，那个残缺是防火墙中断造成的意外而不是设计；**冻结的是方法，不是方法在残缺样本上的取值**，更完整样本上的重算直接替换，不设"明显移动"的阈值（该措辞本身就是错的）。没有下游需要重新验证：clip 率是诊断量、a3 覆盖率仍 100%、三种情形的语义验证不受影响。**一个未兑现的担心如实记下**：架构曾预期补进 XRP/TRX/XLM/XMR/TAO/WLD/TRUMP 等大市值币后 p10 会实质移动，实测只动了 +2.09%，整体分布几乎没变——费率 IQR 的分布在市值维度上相当同质，这是个有信息的阴性结果。

**语义判定三档**（架构裁定，读取前定）：一致事件数 ≥ 10 = 判明；3–9 = 提示性（只记录模式与计数，不判明、不改任何东西，P3 复核）；< 3 = untested；混合 = unresolved（escalate）。**读取范围在读取前固定；事件数落在 3–9 时结论即为"提示性"，不得改用批次 1 去补足事件数。**

**决策读取（只读一次；范围：批次 2 的 12 个文件合并成的 9 个币文件 + ARBUSDT 探测文件；`scripts/check-funding-interval-oracle.mjs`）**
- 可比对 5513 行，一致 5511，**不一致 2**（非拒绝行），被规则 A 拒绝 1。
- **语义事件 15 个：13 个 `period_ending_at_row`（旧,新）、2 个 `lead_new_period`（新,新）⇒ 混合 ⇒ unresolved。**
- 2 个不一致都是 `lead_new_period` 事件所在的行，且都是 1h→4h 切换前的**最后一个 1h 行**：`FUSDT 2025-11-06 04:00`（推断 1h，归档 4h）、`MYXUSDT 2025-09-20 12:00`（推断 1h，归档 4h）。它们不是"与旧周期相等"的滞后形态（`equalsPreviousInferred` 为假）。
- 被拒绝行只有 1 个：`API3USDT 2025-09-21 08:00`，实际间隔 3.000h，名义新周期 4h，**归档写 4h ⇒ keep_rule_A**（与"名义新周期"一致）。
- 其余切换：4h→1h 全部 (4,1)、8h→1h 全部 (8,1)、1h→4h 中 4 次 (1,4)、2 次 (4,4)。
- **预登记决策：escalate**——"2 处非拒绝行不一致：语义/实现问题，escalate；不得为对上而调整规则 A"。**没有调整任何规则；生产仍是规则 A。**

**如实说明这个结果的含义（观察，不是决策）**：1h→4h 切换的 6 个事件里，4 次归档列在切换前最后一个旧行上仍写 1（该行结束的那个周期的长度），2 次已经写成 4（新周期）；4h→1h 与 8h→1h 的 9 个事件全部一致。也就是说这一列在 1h→4h 时的语义**并不统一**。规则 A 与归档一致的比例仍是 5511/5513（99.96%），不一致集中在"最后一个旧行"这一种场合。这与"第一个 4h 周期只有 3h"（API3USDT）出自同一现象：1h→4h 切换点附近的周期长度在币安自己的记录里就有多种写法。**是否影响 a3、影响多大、怎么处置，交架构会话裁定**；本读取不据此改任何东西。

### T16 · 周期 oracle 的独立复现，以及决策记录之后的事后观察（2026-09-21）

**独立复现（批次 1：51 个币 × 2025-08…11；明确标注为复现，不参与、也不能改变 T15 的决策）**：可比对 30,897 行，**全部一致，0 不一致**；语义事件只有 1 个（`TNSRUSDT 2025-11-19 17:00` 4h→1h，(4,1)，period_ending_at_row）；被拒绝行 0 个；复现的读取结论是 untested（事件 < 3）。它证明推断在稳态行上是对的，但对 1h→4h 那两个例外**没有任何信息**（批次 1 的 51 个币几乎没有周期切换）。

**以下全部是 T15 决策记录之后的事后观察（post-hoc）——只描述，不据此改任何东西，也不构成判读。**

1. **1h→4h 切换有三种对齐**（T15 的 6 个 1h→4h 事件加 API3）：(a) 切换落在间隔 4h 的行上（MYX 08-11、MYX 10-12、RED 09-27、SUPER 11-06）：最后一个旧行写 1h，第一个新行写 4h；(b) 新周期的第一次结算恰好落在整点，只隔 1h 就被写成 4h（FUSDT 11-06 04:00、MYX 09-20 12:00，也就是 T15 里的两个 lead 事件）；(c) 只隔 3h 就被写成 4h（API3USDT 09-21 08:00，规则 A 拒绝的那一行）。三种都符合"**归档列 = 该次结算时生效的名义周期**"，而不一定是"实际经过的时间"。
2. **费率是按名义周期报价的**：基准费率是每 8 小时 0.01%（0.00125 %/h）。在 oracle 文件里，三种周期列（1h n=4469、4h n=852、8h n=113）的"每名义小时费率"**众数都是同一个 0.00125 %/h**。切换行 FUSDT 04:00、MYX 09-20 12:00、API3 09-21 08:00 的费率是 **0.0050%**（4h 基准），尽管它们与上一行只隔 1h/1h/3h。所以 `daily = 费率 × 24 / 名义周期` 才对；**按实际经过时间归一化（规则 A 对 1h 间隔行；规则 B 对所有行）会在这类切换行上把 daily 高估 4 倍**（3h 间隔行高估 4/3，规则 A 因拒绝它而不受影响，规则 B 反而会带进来）。
3. **影响面很小，但会随数据量增长**：本地 359 币、221,655 行里，紧邻周期切换（前一间隔 ≠ 后一间隔）的行只有 **31 个（0.014%）**，涉及 22 个币，被规则 A 拒绝的 1 个。当前对 a3 的影响可以忽略；P3 拉多年历史时切换次数会高一个量级（8/9/10/11 月 = 3/5/6/16 次且在加速），届时不能忽略。
4. **规则 A 与 B 都不是对的**：两者都用"实际间隔"，而费率按"名义周期"报价；差别只在切换行上。真正的问题不是 A 与 B 之争，而是**推断无法在切换边界上恢复名义周期**——名义周期既不总等于前一间隔（(b)），也不总等于后一间隔（(a) 的最后一个旧行）。
5. **一个候选规则 C（事后提出，必须在新数据上预登记并验证，不得因本次观察直接采用）**：丢弃"前一间隔与后一间隔吸附后不相等"的行（紧邻周期切换的行），以及被拒绝的行。在已读的 12 个文件上，(a)(b)(c) 三类的可疑行都会被它挡掉，代价是每次切换丢 1–2 行（0.014%）；但"在事后看过的 12 个文件上表现好"不构成验证，需要**新的、未读过的**切换事件（其它月份/币），这需要用户批准的额外下载。

**遵守的纪律**：T15 决策（escalate）已先于复现与本节写入；本节的观察在决策之后产生，任何规则变更都必须先预登记、再在新数据上验证。规则 A 仍是生产规则，未改动。

### T17 · 周期切换边界行的丢弃规则落地（2026-09-21）

**裁定（架构会话；T15 的 escalate 判决原样保留，不做事后重新解释）**：费率按**名义周期**报价（每名义小时费率在 1h/4h/8h 上都是 0.00125 %/h），所以 `daily = 费率 × 24 / 名义周期`；**规则 A 与规则 B 都用实际间隔，两者都错**——预登记问的"A 还是 B"是一个错误的问题，正确答案是"都不是"。这次预登记被严格执行、得出了诚实的 escalate，**真正的答案来自记录之后的事后观察**；先按预登记如实结案、再让事后观察以事后观察的身份入账，两份证据才都可信。预登记防的是"自欺"，防不了"问错问题"，两样都要有。

**为什么不需要新数据验证，而 T16 里我建议要**：规则 C 不是"更好的估计"（性能主张，须在未读过的数据上验证），而是"此处名义周期不可知，所以不估、按缺失处理"（拒绝主张）——正是 R4/R6 的既有原则用在一个新发现的场合。代价有界（每次切换丢 1–2 行 / 约 180 行窗口）；误判方向安全（漏结算同样触发，同样是不可信的行）；判据是逻辑刻画而不是拟合阈值（"前一间隔 ≠ 后一间隔"就是"该行位于边界"的定义）。**无论真相如何都不会更糟的改动，不需要证明它更好。**唯一确实要验证的是**漏检率**（没被捕获的边界行），留作 P3 预登记，届时多年历史里切换事件多一个量级，不需要额外下载。

**实现**（`lib/indicators/features/funding.ts` 的 `normaliseFunding`，生产路径）：一行被丢弃，当且仅当 (1) 它与上一行的间隔不在 {1,2,4,8}h ±10% 内，或 (2) 它与下一行的间隔吸附后 ≠ 它与上一行的间隔（位于周期切换处）。首行无前驱、被丢弃。测试：稳态只丢首行；FUSDT 形态（1h 结算后第一次落在 4h 基准上，费率 0.0050%）的行被丢弃，按实际间隔会得到 0.12%/天而名义周期给出 0.03%/天；一次周期切换只多丢 1 行；第一个正常的 4h 行保留；`checkIntervalInference`、规则 B/被拒绝行判读的既有测试按新行为更新。

**一个必须如实写下的因果性局限（R1，禁止前视）**：判据需要**后一行**，而决策时刻最新一行没有后继。所以规则能修正窗口内部的行（中位数/IQR 以及之后的"当前值"），**修不了"最新一行恰好就是尚未被识别的切换行"的那几个小时**——它被原样使用，直到下一次结算揭示它。这是无法用因果信息弥补的：那一行与普通的 1h 行在当时不可区分。已用测试固定：最新行保留、后继出现后才被丢弃；任意 i 处 a3 与"数组里是否存在之后的行"无关（前缀不变性）。
- **丢弃率**（359 币，W1+前导，221,655 个被判定的行）：**32 行（0.014%）**，22 个币；与预期的 0.014% 一致，判据没有问题。
- **暴露时长**（规则覆盖不了的部分）：W1 内 784,056 个小时级决策点里，有 **34 个（0.0043%）**落在"最新一行之后被证明是边界行"上。这是**上界**：边界行只有在"实际间隔 ≠ 名义周期"时才真的算错（(b)(c) 类），而"最后一个旧行 (a)"其实是对的，两者在当时不可区分。外推到线上约 526 币 × 8760 小时：按 W1 平均切换率约每天 0.5 个币-小时；11 月的切换率约为 W1 平均的 2.7 倍，按那个速率上界约每天 1.5 个——与架构估计的"每天约 1–2 次"在 11 月速率下相容。真正算错（4 倍偏差）的只是其中 (b)(c) 类，约占边界行的 1/3 以下，且只持续到下一次结算。
- **FUNDING_SCALE_FLOOR 随特征定义重测**（方法不变）：非零 IQR p10 = **0.009475**（此前 0.009446，+0.31%）；IQR = 0 占 36.91%；±5 clip 率 5.66%（诊断量）；a3 覆盖率仍 **100.00%**（同 1 个"结算过旧"的点）。按"冻结的是方法"直接替换常量为 0.009475。

### T18 · 已知债务：5 个 UI 测试在干净检出里跑不起来（2026-09-21，仅记录，不处理）

`scripts/test-copy-pools-ui.mjs`、`test-cross-ui.mjs`、`test-indicator-direction-ui.mjs`、`test-indicators-ui.mjs`、`test-square-ui.mjs` 需要外部路径参数，在干净检出里以 `ERR_INVALID_ARG_TYPE` 报错。与本次改动无关，但**在干净检出里跑不起来的测试实际上不是测试**：没人会日常运行它们，它们所保护的东西等于没被保护。属另一条工作线，此处只记债，未修。

### T19 · 完整宇宙上的特征覆盖率（2026-09-21，仅特征侧：无标签、无收益、不联网）

**数据**：服务器回填并拷回本地的 526 个合约（1h 708 万根、4h、资金费率，2024-09-01 至 2026-09-21）。决策点：每个币每天 00:00 UTC 一个，2025-10-01 至 2026-09-21（BTC 一年历史要求 9457 根，2024-09-01 起算最早约 2025-10-01 才可能齐全），共 175,471 个（币，日）。脚本 `scripts/measure-live-coverage.mjs`。

**结果**：21 个特征**同时齐全 95.4%**（167,431 / 175,471）。此前小样本上 e2、e3、g1 只能标"待实际宇宙"，现在有了真实横截面：缺失率 e2 0.1%、e3 0.1%、g1 0.0%（不受 50 币下限限制）。其余缺失率：d4_vol_squeeze_4h 4.2%（历史不足，58.2 天要求）、a3_funding_z 1.7%、4h 结构特征 1.0–1.5%、c1/d1/d2/d3 0.9%（历史不足）、e1 0.4%、其余不足 0.6%。按月：2025-10 88.1% → 2026-08 98.5%（早期低是因为当时较新的币多）。按上市年龄：<60 天 3.6%、60–180 天 99.5%、≥180 天 99.6%。

**"缺失就是缺失"的核对**：覆盖率最低的 GRVTUSDT、DOSUSDT 逐个查了原因，都是上市不满 58.2 天（d4 需要 349 根 4h），不是数据问题；DATAIPUSDT 现已齐全。年龄排除的份额（2026-09-21 当天）：526 个合约里 6 个年龄小于 58.2 天，**1.1%**，低于预登记的复核线 `AGE_EXCLUSION_REVIEW_SHARE` 5%，d4 的 58 天边界无需复核（按（币，日）点数计为 4.3%，包含 2025 年底较新币较多的时期，仍低于 5%）。

**局限（必须随结果一起读）**：只含 2026-09-21 仍在交易的合约。约 121 个在 2024-09 之后已下架的代币永续不在其中（幸存者偏差，约为 526 的 23%）；归档里已下架合约结算后是冻结价的假 K 线，补入必须按 deliveryDate 截断。因此本表描述的是"存活合约"的覆盖率，不能外推到已下架的币；下架前的币往往流动性差、缺口更多，真实覆盖率很可能更低。

**未回测声明**：只是特征可得性的测量，不含任何标签、收益或模型；未验证任何特征的预测力。

### T20 · regime 切点的暂定测量（2026-09-21，仅特征侧；**未冻结任何东西**）

**做了什么**：`scripts/measure-regime-cutpoints.mjs` 在 526 个存活合约上，每天一个值（2025-10-01 至 2026-09-21，356 天），算 g1（市场宽度）与 g2（BTC 波动分位）的三分位切点，并看跨窗口的稳定性。**`CALIBRATION_GATE.regimeCutpoints` 仍为 null**：T11 规定切点"计算一次、冻结为常量"，这是一个需要人来记录的决定，脚本不做。

**暂定切点（全部 356 天）**：g1 ≤ 0.228 为低、≥ 0.579 为高；g2 ≤ 43.2 为低、≥ 63.7 为高（g2 是 0–100 的百分位）。仅用测试窗口（2025-12-01 之后 295 天）：g1 0.258 / 0.577，g2 43.4 / 68.4。前后两半：g1 0.189 / 0.560 对 0.280 / 0.591，g2 47.8 / 72.2 对 37.4 / 57.4，**不稳定**（前后半的差异与切点本身同量级）。g1 与 g2 相关系数 0.007（几乎无关）。

**三个必须记下的发现**
1. **g1 不是"月度 regime"，是很快的日间宽度**：g1 用 EMA60 of 1h 收盘，即约 2.5 天的均线；每个月的 g1 均值几乎相同（0.375–0.528），但每个月内的日度最小最大值都跨越几乎整个 0.03–0.94 范围。它更像"这几天市场是否普涨"，不是"牛市或熊市"。T11 想用 g1 捕捉的"方向性 regime 效应（a3 在一侧饱和）"，g1 未必抓得住。这是规格问题，不是本次能改的，留给架构裁定。g2 有真正的月度变化（2026-02/03 高，2025-10、2026-05、2026-08 低）。
2. **门槛按"有效样本"计数，可能被币的数量虚增**：同一天所有币共享同一个 regime 值，所以每个档位在测试窗口内的**独立天数**只有 g1 低 89、g1 高 98、g2 低 98、g2 高 114（两档低于 100 天），而按（币，日）点数算，每个档位有几万个样本，门槛（minRegimeBinSamples = 100）会被轻松通过。若意图是"测试折覆盖了各种 regime"，应按天数（或权重除以当天的横截面大小）计，而不是按币-日。未改代码，待架构/用户裁定。
3. **幸存者偏差**：g1 只统计 2026-09-21 仍在交易的币（当日可算宽度的币数 407 至 524），历史宽度被高估；一年的 g2 是这份数据的上限（g2 自己需要一整年 BTC 历史）。

**结论**：可以给出暂定数字，但不建议现在冻结：(a) 前后半不稳定，(b) g1 的定义与 T11 的意图有偏差，(c) 已下架合约未补。

**未回测声明**：只是特征分布的测量，不含标签、收益、模型。

### T21 · 幸存者偏差写进门禁；regime 覆盖改按天计；归档假 K 线的截断；4h 聚合对账（2026-09-21，架构裁定）

**裁定 1（幸存者偏差）**：约 121 个 2024-09 之后下架的代币永续（114 个 SETTLING、7 个已不在 exchangeInfo）不在训练数据里，约为 526 的 23%。被排除的不是随机缺失，而是走向归零的那一类，而系统要回答的是"这个币是不是要启动"，最需要的反例正是"看起来像要启动、结果下架了"。偏差方向已知、幅度未知。**已进代码**：`GateThresholds.minDelistedContracts`（默认 null）与 `HeadReport.universe`（`includesDelisted`、`delistedContracts`）。null、报告没写训练集、只含存活合约、数量不足都判失败，原因固定含"训练集仅含存活合约，未补入已下架合约"。补入之前的一切结果都带这句话。

**裁定 3（regime 覆盖按天计）**：同一天所有币共享同一个 regime 值，按（币，日）计数是把一个观测数了 526 遍。`regimeCoverage` 现在按**不重复的天数**计（`days` 参数；同一天出现两个不同的值则整个结果为 null，因为输入与声明不符），阈值改为 `minRegimeBinDays = 30`（判断值，注释已标：是多样性检查，不是统计功效计算；100 天会高于 295 个测试日给三分位的上限约 98 天）。每档实际天数不管通过与否都进报告（W1 后的测试窗口：g1 低 89 / 高 98、g2 低 98 / 高 114）。

**裁定 4**：`minEffectiveN` 是派生量（20 × (类别数 − 1) × (特征数 + 1) = 880），测试折 ≥ 250；`eceMax` 仍为 null，看过可靠性曲线才定。

**归档假 K 线（实测）**：已下架合约在 data.binance.vision 结算之后仍有月度文件，内容冻结在结算价（1000XUSDT 2026-08：744 根，收盘恒为 0.02555，成交量与笔数为 0，sha256 有效）。**截断是代码加测试，不是操作步骤**（`scripts/delisted-trim.mjs`）：① deliveryDate 之前收盘的 K 线才保留；② 不依赖元数据的签名：连续 24 根 volume=0 ∧ 笔数=0 ∧ open=high=low=close，则从首根起全部截断（对已不在 exchangeInfo 的 7 个也有效）；较早者为准；写文件前硬断言没有任何一根越过截断点；每个合约报告截断位置、丢弃根数、原因。资金费率同样按 deliveryDate 截断，没有 deliveryDate 时不能截、并明确报告。测试含整月的假数据、结算日晚于数据、短暂的真实停牌（保留并报告）、整个单合约流程（假归档，无网络）。

**4h 聚合对账（本地，无请求）**：把 526 个合约的 1h 聚合成 4h，与币安自己的 4h 逐根比较（`scripts/check-4h-reconcile.mjs`）：比较 1,771,347 根，**一致 99.9871%**；不一致的 229 根**全部是同一根**（2024-10-28 20:00 UTC，涉及 229 个合约），即币安自己的 1h 与 4h 在那一根上互相不一致（4h 的成交量比四根 1h 之和多约 1.5 亿，开盘价也不同），与聚合方法无关。另有 227 根币安 4h 缺完整的 1h 分组（1h 缺小时），聚合按"缺失就是缺失"丢弃，不补。结论：聚合可用；已知一处交易所侧的不一致（2024-10，早于全部测试折）。

**局限**：只测了 526 个存活合约的一致性；对已下架合约归档里的 1h，聚合是否与币安的 4h 一致无法核对（归档 4h 需另下），首批小批试跑会一并检查。

**（已更正，见 T25）**：此处原先写的"管线内部一律用 1h 聚合出 4h"是错的：线上只采 400 根 1h，聚不出 4h 特征需要的 336 根 4h（要 1344 小时），所以线上只能用币安直接返回的 4h，训练必须用同一个来源（R1）。

**未回测声明**：本条是数据处理与门禁的机械约束，不含任何模型或收益。

### T22 · regime 轴的持续性尺子（游程中位数）；覆盖率门禁（2026-09-21，架构裁定；**未冻结任何切点**）

**裁定**：(1) `minDelistedContracts` 改为覆盖率 `minDelistedCoverage`（判断值，建议 0.90，发货默认 null），外加"每个未取得的合约必须有记录在案的原因"；报告必须带认识论边界：覆盖率针对**已识别的集合**（114 个 SETTLING 加另行找到的 7 个），完全从 exchangeInfo 消失的合约可能更多、无从枚举，所以不是"全部下架合约"（`GATE_NOTES`，每次 `evaluateGate` 的结果无论通过与否都带这一条）。(2) g1 拆成两个角色：**g1 保持为特征，不做 regime 轴**（FEATURE_VERSION 不变）；regime 轴改为只依赖 BTC 的两个量：`trend`（BTC 尾随 30 天收益，%，方向）与 `vol`（即 g2，BTC 波动分位）。因为只依赖 BTC，补入已下架山寨合约不会改变它们，所以它们的切点**不必等补完下架合约**。(3) 验收尺子改为**游程长度**：同一档位连续天数的中位数 ≥ 14 天（`MIN_MEDIAN_RUN_DAYS`，判断值：折跨周到月，游程约两周时 regime 才是折级别的属性；一两天的游程是日内噪声，覆盖检查会自动通过）。**先用 g1 验尺子**（已知的坏例子，中位数应为 1–3 天），再量候选；月度三分位分布保留作佐证，不当门禁。代码：`regimeRuns`、`persistsAsRegime`、`btcTrailingReturnPct`，测试里有已知坏（无记忆序列，中位数 1–3）与已知好（月长块，中位数 30）的两个合成序列，恰好 13 与 14 天的边界。regime 轴在代码里从 g1/g2 改名为 trend/vol。

**测量**（`scripts/measure-regime-persistence.mjs`，526 个存活合约里只有 BTC 用于 trend 与 vol；g1 用全宇宙宽度）：
- **尺子自检**：g1 的游程中位数 **1 天**（落在预期的 1–3 天内）→ 尺子能筛掉已知坏例子，可以使用。
- **trend（BTC 尾随 30 天收益）：不合格。** 720 天，切点 ≤ −2.641% / ≥ 5.966%，共 108 段，**游程中位数 2 天**（均值 6.7，最长 52 天）。月度分布佐证：有些月份高度集中（2025-11、2026-02、2026-06 是 100% 低；2024-11、2025-05 是 100% 高），但很多月份跨多个档位；中位数被"在切点附近来回跳"的很多短游程压低。前后两半的切点也不稳（下限 −0.66 对 −6.18，上限 9.30 对 3.52）。
- **vol（g2）：刚好合格，且很脆弱。** 356 天，切点 ≤ 43.242 / ≥ 63.699，21 段，**中位数恰好 14 天**（均值 17.0，最长 58；低档 20、中档 8.5、高档 28）。只有 21 段，多一段或少一段就会改变中位数；前后两半的切点也不稳（下限 47.8 对 37.4，上限 72.2 对 57.4）。

**没有冻结**：架构裁定的规则是"合格才冻结"。trend 不合格，只有 vol 合格且恰在门槛上，而门禁要求两个轴同时有切点，所以**冻结留给架构复核**；**没有事后调整尺子**（中位数的定义、14 天、三分位都不改）。可能的补救（不是本条决定，留给架构裁定，任何一条都要在看数据之前预登记）：对 trend 换更长的窗口（如 90 天收益）、或用带滞回的分档，或改用"经历该档的天数加权的游程中位数"（按天而不是按段，会大幅降低短游程的影响）。

**局限**：g1 与全宇宙相关的部分只含存活合约；trend 与 vol 只依赖 BTC，不受影响。**仅特征分布的测量，不含标签、收益、模型。**

### T23 · 完整宇宙上的标签分布（2026-09-21，仅存活合约，**未重新冻结 k**）

**做了什么**：`scripts/measure-label-distribution-live.mjs`，在 526 个存活合约、全部历史（2024-09 至 2026-09，每 6 小时一个决策点、从第 349 根 K 线起，与特征同一预热）上，按**已冻结的规则** `k_h = 1.0 × sqrt(h/4)`（4h 用 k=1.0，24h 用 k=2.449）、成本按假设的往返滑点 0.001 加各币**当前**的点差（只知道今天的点差，整个窗口都用它，这是**乐观**的代理），算三重障碍的三类占比。不含模型、特征、网络。

**结果（4h，n=1,150,342）**：down 30.6%、flat 39.7%、up 29.7%，同 bar 双触 0.5%；**24h（n=1,148,435）**：down 35.9%、flat 28.5%、up 35.5%，双触 0.1%。两个视野都在 15% 到 60% 规则之内。滑点敏感性（往返 0.0004 / 0.001 / 0.002）：4h flat 37.0% / 39.7% / 43.9%，24h flat 27.5% / 28.5% / 30.3%，仍在规则内。**按季度**：4h 的 down 在 27.4%（2026Q3）到 34.1%（2024Q4）之间，24h 的 down 在 31.1% 到 38.3% 之间，up 在 33.3% 到 43.1% 之间；季度间漂移存在，方向随行情变化（2024Q3 的 24h 是 up 43.1% 对 down 31.7%，2025Q4 是 down 38.2% 对 up 33.3%）。

**必须一起读的局限**：只含存活合约。下架的币偏向"跌"类，补入后 down 的占比必然上升，分布会移动。**所以不重新冻结 k**：冻结的是方法（k 的公式与 15% 到 60% 的判据），不是方法在残缺样本上的取值；补完下架合约之后按同一方法重算，直接替换（同 `FUNDING_SCALE_FLOOR` 的做法）。点差是今天的，历史上更宽，所以 flat 的占比在真实成本下更高。

**未回测声明**：只是标签类别占比的描述，没有模型，没有任何收益结论。

### T24 · regime 切点冻结（2026-09-21，架构裁定的预登记路径；结论如下）

**路径**（架构在我看到 trend 的按段结果之后、在测按天加权统计之前预登记）：第 0 步先用 g1 验按天加权统计（g1 必须远低于门槛，至多 7 天，否则视为搬门柱、放弃并回到按段中位数）；第 1 步 trend 与 vol 都用两个统计量并排测；trend 不合格才测**唯一的**第二个候选（BTC 尾随 90 天收益，测一次）；两个都不合格则只保留 vol，并把"方向性 regime 未建立"写进门禁报告。

**结果**：
- **第 0 步**：g1 按段中位数 1 天、**按天加权中位数 2 天**（≤ 7）⇒ 按天加权统计有效，成为决定性统计。
- **trend**（BTC 尾随 30 天收益，720 天，108 段）：按段中位数 **2 天**（未达 14），按天加权中位数 **18 天**（通过）。**仅在按天加权下合格**：切点附近有大量短游程，但一个随机的天多半落在长游程里。因此通过，**不需要测第二个候选，BTC 尾随 90 天收益没有被测**。
- **vol**（g2，356 天，21 段）：按段中位数**恰为 14.0**（擦线，只有 21 段，增减一段即可改变按段的结论），按天加权中位数 **30 天**（通过）。

**冻结的切点**（`FROZEN_REGIME_CUTPOINTS`，全部可用天数上的三分位，只依赖 BTC，不受补入下架合约影响）：trend 下限 −2.6411486578998353、上限 5.965780629414542（%）；vol 下限 43.242009132420094、上限 63.6986301369863。**"高""低"是相对整个两年窗口而言，不是相对任何局部时期**：前后两半各自算出的切点差别很大（trend 下限 −0.66 对 −6.18、上限 9.30 对 3.52；vol 下限 47.8 对 37.4、上限 72.2 对 57.4），说明底层分布在漂移，这是关于市场的信息，不是方法的缺陷。

**门禁里如何显示这些事实**：`REGIME_PROVENANCE` 与 `regimeNotes()` 生成的说明进入 `GATE_NOTES`，**每次 `evaluateGate` 的结果都带**（通过与否）：决定性统计是按天加权；trend 仅在按天加权下合格（按段 2 天）；vol 按段中位数恰为 14.0、只有 21 段、增减一段即可改变按段的结论；"高低"相对整个两年窗口。`CALIBRATION_GATE.regimeCutpoints` 不再是 null（发货默认现在带冻结的切点），其余 null 字段（`eceMax`、`minClassSamples`、`minDelistedCoverage`）仍使默认门禁不可能通过。

**局限**：两个轴各只有约 1 到 2 年（vol 356 天）；trend 的通过依赖按天加权这个统计量的选择（在看到按段失败之后预登记，并用 g1 验过）；已如实并排报告两个统计量。

**未回测声明**：只是特征分布的测量与门禁常量的冻结，不含标签、收益、模型。

### T25 · 4h 的训练/线上同源（R1）；聚合对账降为诊断；泄漏对照保持时间结构；最终门禁取值（2026-09-21，架构裁定）

**问题（我在 T21 里提了一个错的方向，架构指出）**：4h 特征在线上必须用币安直接返回的 4h（线上只采 400 根 1h ≈ 16 天，聚不出 336 根 4h 需要的 1344 小时）。所以训练里用"1h 聚合出的 4h"就是**训练与线上不同源**：模型训练时见到的 4h 与线上喂给它的不是同一个东西，而且失效方式完全静默。聚合与币安 4h 在 229 根上不同（2024-10-28 20:00，见 T21），这就是那个不同源的具体大小。

**裁定与实现**：训练侧只读**回填下来的币安 4h**（先查了：特征管线目前读的就是它，文件 source 是 `fapi/v1/klines`，不是聚合，所以已经符合，没有需要改的既有数据）。聚合出的 4h 不进数据通路：
1. **`scripts/kline-source.mjs`**：`assertNativeKlines`——文件的 `source` 必须是交易所 API 或公开归档；含"aggregat"就拒绝（即使同时提到 API）；没有 source 或来源不明也拒绝；周期不符拒绝。读 4h 的四个特征侧脚本（measure-live-coverage、measure-regime-persistence、measure-regime-cutpoints、rehearse-pipeline）都过这道检查。
2. **测试（`scripts/test-4h-source.mjs`，静态扫描，不运行脚本）**：数据通路里没有任何脚本 import 聚合器（只有对账诊断和测试可以）；任何读校准 4h 的脚本必须调用 `assertNativeKlines`；扫描本身有阳性对照（必须看到对账诊断在用聚合器；覆盖 50 个以上的文件）。我用一个临时的违规文件做了两次突变检验（import 聚合器；读 4h 却不检查来源），测试都变红，删除后恢复。
3. **已下架合约的 4h 也下交易所自己的 4h**：`fetch-delisted.mjs` 不再聚合，而是同样从归档取 `4h` 月度文件并同样截断（4h 的冻结签名用 6 根 = 同样的一天）。**小批的请求数因此增加**（每个合约的每个月份多一个 4h 文件，各带 checksum）。已告诉运营会话。
4. **聚合对账脚本保留，性质改变**：从"验证聚合可用"变成"监测币安 1h 与 4h 的内部一致性"，是诊断工具，不在数据通路上。
5. **代价**：币安 4h 缺完整 1h 分组的那 227 根，按缺失处理，不用聚合去填——填了就是往训练集里放线上永远不会出现的数据。

**泄漏对照的粒度**：打乱标签的对照必须保持时间结构、只打乱标签与特征的对应关系。整体置换会破坏重叠标签的相关性，藏在这个结构里的泄漏会被漏掉（假阴性）。已实现 `blockShiftLabels`：每个币的标签沿它自己的时间轴旋转四分之一到四分之三的长度，保持每个币的标签多重集与游程结构（最多多一个接缝），只打断与特征的对齐；测试对比了整体置换会把游程打碎（接缝多三倍以上）。管线演练（`scripts/rehearse-pipeline.mjs`）用的是它。

**`minDelistedCoverage = 0.90`**（架构确认为最终值）：写入发货默认，判断值；测试断言默认门禁拒绝只含存活合约的训练集。`eceMax` 与 `minClassSamples` 保持 null（eceMax 必须看过可靠性曲线才能定）。

**局限**：标签分布随季度漂移（4h 的 down 占比 27.4% 到 34.1%，24h 的 down 31.1% 到 38.3%），说明标签分布随行情变，这正是 regime 覆盖要管的事，无动作。**"仅存活合约"与"点差用今天的"两个偏向都让结果偏乐观**，必须一起列在所有结果的局限里：存活合约偏向乐观（缺失走向归零的币）；今天的点差比历史窄，历史成本下 flat 更多。

**未回测声明**：数据来源的机械约束与对照的构造，不含模型或收益。

### T26 · 管线演练（2026-09-21，**不是结果**：只验管线与对照是否正常；无阈值被设置或调整）

**做了什么**：`scripts/rehearse-pipeline.mjs` 在 526 个存活合约上，24h 视野（k = 2.449），每 3 天一个决策点（2025-10-01 起，21 个特征齐全且有标签的 55,943 个样本），4 折的前向切分（清洗加禁闭，全部测试块晚于 2025-12-01）、softmax 拟合、随机特征基线（每折 20 次抽样）、保持时间结构的标签对照（每个币沿自己的时间轴旋转标签）、regime 覆盖（冻结切点，按天计）、并把报告交给发货默认的门禁。

**管线本身正常**：切分违规 0；随机特征基线在 0 附近（各折均值 −0.0021 至 −0.0003，p95 至多 0.0001）；打乱标签后的得分在 0 附近（−0.0009 至 0.0001），即标签与特征的对齐被打断后分数塌到噪声水平；门禁按预期**拒绝**，并给出全部原因（含"训练集仅含存活合约，未补入已下架合约"，以及三条进入每个结果的 GATE_NOTES）。

**演练中看到的、不是结论的数字**：真实特征的 BSS 各折为 −0.0327、−0.0287、+0.0101、+0.0085（训练集从 7,917 增到 42,607 个样本）；池化 −0.0098，ECE 0.0813。**不要引用**：折数少、训练窗口很短（2 个月起）、未调任何超参（L2=0.05 固定）、每 3 天抽样、只含存活合约；早期折为负、后期折略为正，只说明小训练集上一个 22 参数的模型并不稳，与"特征有没有预测力"无关。泄漏对照的结论是 `inconclusive`（真实模型没有超过随机基线，没有信号可供打乱去摧毁），这是设计中的正确行为，不是通过。

**演练暴露的两点管线缺口（不是数据问题）**：(1) `residualBss` 没有计算（门禁列为缺失），评估管线里还缺一个 BTC 贝塔调整后的标签路径；(2) 这次每 3 天抽样，regime 覆盖里 trend 高档只有 28 个不同的测试日（< 30，被门禁拒绝）：全部按天取样时各档约 35 至 40 天，这提醒**天数门槛对采样密度敏感**，正式评估必须每天取样。

**未回测声明**：只是管线的演练。没有任何模型被认证，没有阈值被调整；所有数字仅存活合约、点差用今天的，偏乐观。

### T27 · 已下架合约小批试跑的核对（2026-09-21；原始月度文件在 CypeData/data/calibration/delisted/tmp）

小批：KLAYUSDT、PORT3USDT、VICUSDT、EOSUSDT、MATICUSDT，用 d04acd05 的脚本在服务器上跑（1h 加 KLAY 的资金费率，无拒绝）。那批的 4h 是旧版 1h 聚合的，**作废**（R1，见 T25），用 `--only-4h` 重下。逐根核对原始数据（不需要请求）：

- **MATICUSDT（保留 81、丢 169）：规则没有误伤，正好截在真实停止交易的那一小时。** 2024-09-04 08:00 那一根是最后一根真实的（成交量 3698 万、29,800 笔，价格从 0.391 打到 0.376），**09:00 起整整 169 根（到 09-11 09:00）全部是 volume=0、笔数=0、四价恒为 0.3846**；09-05 到 09-11 每天 24 根全部冻结。这是没有 deliveryDate 的"已消失"合约，只靠签名截断，结果与真实停止交易的时刻吻合。我原先"几乎不丢"的预期是错的（MATIC 在那个月迁移成 POL，币安在 09-04 停了这个合约，之后归档里只有填充）。
- **PORT3USDT（结算日 06:30，输出显示 06:00）：设计行为，文案误导已改。** 输出里的时间是"第一根被丢弃的 K 线的开盘时间"，不是结算日。包含 06:30 的那根 [06:00, 07:00) 有真实成交（成交量 3.93 亿、67,659 笔，价格 0.01355 到 0.0118，低点 0.0092），07:00 起才是冻结的 0.01182。之后 186 根里只有那一根有成交。**代价必须说清**：丢掉这根不完整的 K 线，就丢掉了结算前最后半小时的真实下跌（低点比开盘低 32%）；这是"宁可少半小时、不用不完整的 K 线"的取舍。**没有改**，留给架构裁定是否保留含结算时刻的那一根（它的成交是真实的，但它的收盘不是一个完整小时的收盘）。
- **EOSUSDT（"frozen bars kept inside 2"）：两根不同性质。** (1) **2024-10-28 20:00**：夹在真实 K 线之间的一根零成交平价（前一根成交量 348 万，后一根 273 万）——这正是 T21 里"币安 1h 与 4h 在 229 个合约同一根上不一致"的那一根：1h 那一小时是被填成零的洞，而覆盖它的 4h 有成交量。是交易所侧的数据洞，不是假 K 线也不是真停牌；保留（它在 2024-10，早于全部特征窗口）。(2) **2025-05-21 09:00**：归档最后一个文件的最后一根，前一根（08:00）成交 13,308 笔，这一根是零成交平价，之后文件结束：是**结算那一根的填充**，一根假 K 线。它只有 1 根，24 根的签名规则永远抓不到。
- **由此新增规则 3（结尾的冻结 K 线，任意长度）**：一个序列不可能以没有成交的 K 线结尾，所以规则 1、2 之后，把序列最末尾连续的冻结 K 线（任意长度）去掉；冻结 K 线后面还有真实 K 线的（如 2024-10-28 那个洞）不动。测试覆盖：EOS 那种单根结尾、5 根结尾、结尾冻结前有停牌保留、全是冻结则全丢、与规则 1、2 落在同一位置时不重复计数。**这是我在看到数据后加的规则**，加法性质（只去掉零成交平价的尾巴），已如实标注，请架构复核。
- 归档提供已下架合约的资金费率（KLAYUSDT：155 行，结算日前 28 行之后被截掉）。

**未回测声明**：数据清洗规则的核对，不含模型或收益。

### T28 · 已下架合约小批：交易所 4h 与 1h 聚合的逐时间戳比对（2026-09-21，处理后的最终文件；不联网）

比较 5 个合约处理后的 1h 文件聚合出的 4h 与交易所自己的 4h（`scripts/aggregate-4h.mjs`，仅诊断，不在数据通路上）：
- **KLAYUSDT（308 对 308）、PORT3USDT（1031 对 1031）：逐根完全一致**，无任何差异。
- **VICUSDT（3076 对 3075）：多的一根是 2025-03-12 16:00 的 4h 块。** 那个块里 1h 只有 17:00、18:00、19:00 三个小时，**16:00 这一小时在归档的 1h 里缺失**，聚合因分组不完整而丢弃，交易所的 4h 仍有这一根（成交 2.09 亿、48.2 万笔）。即：归档 1h 里有一个缺失的小时，这是交易所数据本身的缺口，与截断规则无关。
- **EOSUSDT（1575 对 1574）：** 多的一根是 2025-05-21 08:00 的 4h 块，它只有 08:00 这一个小时有真实成交（13,308 笔），其余小时是冻结填充，被 1h 的规则 3 去掉，聚合因此丢掉这块；交易所的 4h 保留它（含真实成交，是合法的一根，保留）。另有一根公共 4h 只在 `open` 一个字段上不同：正是 2024-10-28 20:00 那个块（1h 那一小时被零填充，聚合取到填充价作开盘，交易所的 4h 开盘是真实的），即 T21 里那一处交易所侧的 1h 与 4h 不一致。
- **MATICUSDT（21 对 20）：** 多的一根是 2024-09-04 08:00 的 4h 块，同 EOS：只有 08:00 一个小时有真实成交（成交量 3698 万、29,800 笔），之后是冻结填充。

**结论**：之前"聚合丢了不完整的分组"的猜测对了，且逐根验证过：交易所 4h 比聚合多出的每一根，要么是 1h 里缺了一个小时（VIC），要么是停止交易那一块只有第一个小时有成交（EOS、MATIC），都是交易所的真实数据，训练用交易所的 4h 是对的（R1）。截断规则对 4h 的效果符合预期：4h 按结算日丢弃包含结算时刻的那一块（PORT3、VIC、KLAY 的 4h 都在结算前一块结束）；停止交易的最后一块（含 1 个真实小时）保留。

**未回测声明**：数据核对，不含模型或收益。

### T29 · 架构对 T27/T28 的裁定：规则 3 批准；结算那半小时按角色拆开；residualBss；按天取样断言（2026-09-21）

**规则 3（末尾冻结 K 线）：批准。** 依据（架构）：24 根签名的作用是区分"结算冻结段"与"短暂真实停牌"，但**在序列末尾这种歧义不存在**——后面没有东西了，不可能是"停牌后又恢复"，所以尾部不需要 24 根的证据强度，1 根就够；只动最末尾、冻结段后面还有真实 K 线的（2024-10-28 那个零填充洞）不动，这一限定是关键；误判代价有界（万一有一根成交为零的真实末根，损失 1 根），拒绝性规则的验证负担低于估计性规则。**它是我在看到 EOS 数据之后补的**，不是预先想好的；这一点如实保留。

**结算不在整点的合约：保留那根不完整的小时，但拆开它的两个角色（架构）。** 之前我按"不完整的一根不要"把它丢了（PORT3：包含 06:30 结算的 [06:00,07:00)）。架构指出这是自相矛盾：补入已下架合约的目的就是把"最坏的结果"放回训练集，而"结算前半小时暴跌 32%"正是它的极致形态，丢掉它等于在修正幸存者偏差的同时又系统性地删掉每个下架合约最坏的那一根。裁定按角色分开：**标签路径（前瞻价格）保留**（成交是真实的，最后价就是最后价）；**决策点与特征输入永不使用**（它不是一根完整的小时；合约已结算，也没有决策点会落在它上面）。实现：`trimDelisted(..., { keepPartialLast })` 只对 1h 开启（4h 没有这样的使用者，保持只留结算前已收盘的块）；文件记录 `trim.partialLastBarOpen`；`partialLastIndex` 与 `decisionEligible` 是给使用者的判据（决策点不得落在这一根上）；输出文案写明"the last bar … is a PARTIAL hour: label path only, never a decision point"。规则适用于**所有结算时刻不在整点的合约**。

**这个改动影响多大（报告）**：
- 计划里 114 个 SETTLING 合约中只有 **3 个**结算不在整点（都在 :30）：OBOLUSDT（2025-12-05 09:30）、PORT3USDT（2025-11-23 06:30）、TOKENUSDT（2025-12-05 09:30）。**不是几百个，是 3 个合约。**
- 每个合约每个视野最多多出 **1 个有标签的决策点**（多出的那一根让 t = 终点 − H 的窗口变完整）。PORT3 实测（原始月度文件，含与不含那半小时）：4h 视野多出 1 个（2025-11-23 03:00，标签 up）；24h 视野多出 1 个（2025-11-22 07:00，标签 **down**），其余 496/516 个决策点的标签完全不变。合计最多 6 个样本；方向是系统性的（多出的是下架前最后一天的下跌），这才是它值得改的原因，不是因为量大。
- 已下载的文件：批 1 之前下的小批里只有 PORT3 缺这一根（OBOL 在批 2、TOKEN 在批 4，尚未下载，会带着新规则下）；PORT3 只需重下 1h 的 6 个月份（12 个请求，`--only-1h`），需要用户批准。

**一个比这半小时大得多的结构性发现（提给架构裁定，未改）**：任何合约的**最后 H 根之内的决策点没有标签**，因为前瞻窗口不完整（标签规则"窗口不完整则为 null"）。对已下架合约，这意味着**结算前最后 H 小时的每个决策点都被丢掉**：121 个已识别合约 × 24 = **2,904 个 24h 视野的决策点**（4h 视野 484 个）没有标签——而这恰是"崩盘进入下架"的那一段。这是与半小时同一个方向、但大两个数量级的系统性缺失。可能的处理（不由我选）：结算价作为终点（窗口到结算为止，没触及障碍则按结算价相对入场的收益归类），需要一个预登记的定义。

**residualBss（架构：做，否则那条门禁是空的）**：`lib/calibration/residual.ts` 的 `residualLabelAt`，评估侧标签（不是特征，不会进模型）；用与特征 e1 **同一个**贝塔估计（`estimateBeta`，从 e1 里抽出成唯一的实现，e1 改为调用它，特征测试不变），同样的 k、成本、ATR，只用收盘价；任何缺失（贝塔估不出、前瞻窗口里缺 BTC 的一根、ATR 无效、窗口不完整、成本缺）都返回 null，不用最近的一根代替。测试：一个恰为 BTC 平方的币贝塔约为 2，它只是跟着市场走，贝塔调整后的路径是 flat，而同一窗口的普通标签不是 flat（这正是残差检验要去掉的东西）。

**按天取样断言（架构：写成断言，不是注释）**：`regimeCoverage` 现在输出 `samplingIntervalDays`（不重复的测试日之间最常见的间隔），门禁在它不等于 1 时**直接拒绝**（原因写"测试日不连续，天数门槛不可比"），不论各档天数是多少。测试覆盖 2、3、7、0、null、NaN、字符串，以及"各档有一万天但每 3 天取一个"仍被拒绝。

**未回测声明**：数据处理规则与评估管线的机械约束，不含模型或收益。

### T30 · 结算时间障碍：结算前最后 H 小时的标签（架构预登记，2026-09-21；规则一字不改）

**先记一笔（架构对自己的纠正）**：T29 里"结算不在整点的合约影响几百个"是架构的预估，实测只有 3 个合约、最多 6 个样本，高估了两个数量级；改动仍然做对了（方向是系统性的），但量级预估是错的。

**问题**：标签规则"前瞻窗口不完整 ⇒ null"对存活合约是对的（数据还没长出来，确实不知道），但对已结算合约，最后 H 小时的决策点全被丢掉：121 个已识别合约 × 24 = 2,904 个 24h 视野的决策点（4h 视野 484 个）没有标签，而这恰是"崩盘进入下架"的那一段。

**预登记的规则（冻结；一个字都不改）**：若该合约有**真实的 deliveryDate**，且 t + H 越过结算时刻：(1) 障碍照常在 [t, 结算时刻] 内检查；(2) 先触上障碍 ⇒ up，先触下障碍 ⇒ down（与普通标签完全一致）；(3) 到结算时刻仍未触及任何障碍 ⇒ flat。即结算时刻充当一个**提前到来的时间障碍**；k、成本、ATR、双触规则全部与普通标签一致，**不引入任何新参数**。

**为什么不是补丁**：存活合约的窗口不完整是"未知"；已结算合约的结果是"已知"（仓位在结算价被强平）——这是 R4/R6 一贯的分界，并且它准确地模拟了交易者的真实遭遇。**不要**改成"结算价相对入场的收益"另立分类：那会引入没有依据的新阈值（多少算涨、多少算 flat）。暴跌先触下障碍，直接判 down，走的是普通逻辑；真正落进第 3 条的是"没怎么动就结算了"，判 flat 语义正确。

**没有真实 deliveryDate 的合约（7 个已从 exchangeInfo 消失）：规则不适用，标签保持 null**。理由是主张的性质不同：用冻结签名截断数据是**拒绝**（丢弃可疑数据，误判只损失几根），用推断的终点给标签定性是**断言**（这就是结果，错了就是错误标签进训练集）。推断的终点可以截断，不可以断言结果。7 个合约 × 24 ≈ 168 个点如实记录为缺失。

**实现**：`tripleBarrier` 的 `settlementMs` 选项与结果里的 `settled` 标记；只在 bars 真的止于结算（最后一根在结算前开盘且到达结算）时适用，否则窗口不完整照旧为 null；结算前只剩一个决策点之后没有 K 线则为 null（"no bar after the entry"）；窗口完整的决策点即使属于已结算合约也是普通标签（`settled=false`）；缺口、成本缺失照旧为 null。`splitBySettlement` 把普通标签与结算截断标签的类别分布**并排**报告，不混在总分布里。测试 `scripts/test-settlement-label.mjs`：暴跌判 down、没触及判 flat、上障碍、双触规则、与普通标签在触及时逐项相等（k、成本、ATR 相同）、各种不适用情形（无 deliveryDate、NaN、结算远在数据之后、结算早于最后一根、决策点在最后一根）、不完整的最后一小时（半小时结算）、完整窗口不受影响、缺口仍为 null、并排统计。

**在已下载的小批上的分布（`scripts/measure-delisted-labels.mjs`；只有 5 个合约，数量小，形状不能下结论；点差用存活合约的中位数 4.10 bps 代替，成本 0.241%）**：普通标签（4h，n=22,548）down 30.5% / flat 40.7% / up 28.8%，与存活合约的分布（T23：30.6 / 39.7 / 29.7）基本一致；结算截断标签（4h，n=6）全是 down；24h 视野普通 33.8 / 31.0 / 35.2，结算截断（n=46）down 34.8% / flat 65.2% / up 0%。**样本太小，只说明机制在跑、两类被分开统计**；**PORT3 一个结算截断标签都没有**：它已下载的 1h 文件缺结算所在的那半小时（旧版丢了它），bars 没有止于结算，规则按设计不适用，24 个决策点保持 null，需要用 `--only-1h` 重下（这正是检验"规则只在数据真的止于结算时适用"）。已消失的 EOS、MATIC 不适用（EOS 24h 视野 24 个决策点、4h 视野 4 个保持缺失）。

**局限**：分布是描述，不含模型；只含已取得的 5 个下架合约；补完后重算并把结算截断标签的分布与普通标签并排看，若差异悬殊，那本身是需要知道的事实。

**未回测声明**：标签定义与描述性统计，不含模型或收益。

---

### T31 · 断点续存的静默失败（2026-09-22，自查发现并修复）

`loadCheckpoint()` 用了 `readFileSync`，但导入列表里只有 `existsSync, renameSync, writeFileSync as writeFileSyncFs`，漏了 `readFileSync`——调用即抛 `ReferenceError`，被我自己写的空 `catch {}` 吞掉，每次都静默返回 `{}`：程序不报错、看起来在续存，实际每次都从零算起。**用哨兵值自查发现**：写入含哨兵值（−999、−888）的假 checkpoint，删掉中间一个 k，重跑，确认日志打出"resuming from checkpoint... 2/3 draws already on disk"且哨兵值被原样保留、缺的那个被正确按其确定性种子重算。修复：补上 `readFileSync as readFileSyncFs` 的导入，`catch` 改为打印捕获到的错误而不是静默吞掉。已提交（`fbce3b85`）。

**未回测声明**：工具代码修复，不含模型或收益。

### T32 · 已下架合约"obtained"的定义（2026-09-22，架构裁定，T32/T33 一并讨论确定）

"已获取"不等于"文件已下载"，也不等于"窗口内有行"：一个合约只有在**至少贡献一个通过 R3（21 个特征全齐）并且拿到标签的样本**时才算 obtained，否则记为 unobtained 并写明原因（窗口内无真实交易数据 / 有 K 线但无资金费率历史导致 R3 全部排除 / 有数据但未贡献任何可用样本）。`HeadReport.universe` 承载 `identifiedDelisted / obtainedDelisted / unobtained`（每条附原因）。

### T33 · 含下架合约演练的强制拆分报告（2026-09-22，架构预登记，先于看到结果）

无论结果如何都必须报告五项：(1) 两组 BSS + 按天聚类自助法 CI；(2) 下架合约贡献的训练/测试行数（绝对值+占比）；(3) 已下架行 vs 存活行拆分的 BSS/ECE（同一个池化 base rate）；(4) 因 `a3_funding_z` 缺失被 R3 排除的真实规模；(5) obtained/unobtained 最终计数与逐条原因。预登记的读法：池化 BSS 下降 = 符合预期（幸存者偏差被诚实地暴露）；持平 = 检查下架行的贡献占比是否小到不可能推动池化数字；上升 = 强制做拆分排查，怀疑数据artefact 而非真实发现。

### T34 · 下架合约价差 bug：发现、根因、修复、三档敏感性（2026-09-22/23）

**现象**：含下架合约的完整演练跑完后，下架合约贡献样本 0/166912=0.00%、obtained 0/121，与纯存活合约那次的数字逐位相同。

**排查**：确认 121 个文件被正确加载、R3 正确排除 4 个资金费率缺失的合约（365/365）；加临时诊断发现其余 ~12640 个通过 R3 的下架决策点里 96%（12127 个）在打标签阶段变成 null；单独用 OBOLUSDT 在给定真实价差时 65/65 决策点全部正常出标签，排除了 settlementMs 分支或数据缺口的问题。

**根因**：`rehearse-pipeline.mjs` 里 `cost = roundTripCost({ spreadBps: c.spread, ... })`，`c.spread` 取自**当前实时**指标快照里按 symbol 查的买卖价差字典——已下架合约早就不在这份实时快照里，`c.spread` 恒为 `null`，`roundTripCost` 返回 `null`，`tripleBarrier` 因"cost missing"对几乎所有下架决策点返回空标签。这是集成 bug，不是幸存者偏差方向的真实发现。

**裁定与修复**：替代价差取**存活合约当前价差分布的 p90**（判断值，不臆造；理由：已下架合约按定义是流动性尾部，中位数会低估其真实成本，让它们显得比实际更可预测，与补入它们消除乐观偏差的目的相悖）。三档敏感性 p50/p90/p99 必做，预登记判读规则：若结论在三档间翻转则报"无法判定"。

**结果（p50/p90/p99，全量，horizon 24h）**：池化 BSS（plain）0.0071 / 0.0072 / 0.0073，跨度 0.0002，结论不翻转；残差 BSS 三档几乎不变（0.0159 左右）。下架合约贡献 12125/179037=6.77%（train+test）、测试集 7892/150171=5.26%；R3 因 `a3_funding_z` 缺失排除 365/365（EOS/MATIC/PORT3/VIC 四个合约）；obtained 81/121=66.9%（三档一致）。**已知局限（两边都软，独立记一条）**：存活合约用"今天的价差套两年历史"（偏乐观，历史流动性更差）；已下架合约用一个选定的替代值（p90，方向保守但仍是判断值）。

### T35 · 已下架 vs 存活行的 BSS 拆分排查（2026-09-23，架构预登记，触发条件事后发现设错）

架构原定触发条件是"池化 BSS 上升才排查"，但下架行仅占池化测试集 5.26%，无论其表现如何都推不动池化数字——**触发条件本身看不见它要抓的信号**，判定为条件设计错误，排查照做，与池化方向无关。

**两种 base rate 口径**：对池化 base rate，下架行 BSS 0.0130（n=7892）vs 存活行 0.0046（n=142279），约 3 倍；对各自子群自己的 base rate，下架行 0.0074 vs 存活行 0.0046，约 1.6 倍——**此前观察到的"3 倍差距"大部分是基础率差异造成的假象**（与 `residualBss` 当初栽的坑同源）。

**(a) 按标签类型**：结算截断标签 BSS −0.1019（n=66，很小），并未抬高成绩，反而更差——排除"结算障碍让标签系统性更容易"的假设。

**(b) 按真实类别**：down 下架 0.1054 vs 存活 0.0894；flat 下架 0.1565 vs 存活 0.0822；**up 下架 −0.2795 vs 存活 −0.1556，两者都是负的**——按类别 BSS 在此处有解释陷阱（条件于结果的统计量，整体校准良好的模型也可能单类为负），**不能仅凭此判定"模型挑不出上涨候选"**，需要十分位提升度做决定性检验（见下）。

**(c) 按距结算天数分桶**（下架行，仅含有真实 deliveryDate 的 7784/7892 行）：>90 天 0.0159、30-90 天 0.0156、7-30 天 0.0121、<7 天 **−0.0258**——离结算越近表现越差，不是越好，**排除"模型在识别快死的币"这个担忧的方向**。

**十分位提升度检验（决定性）**：按预测 p(up) 排序，最高十分位实际 up 率 41.5%（plain）/31.3%（残差）明显高于基础率 36.2%/23.8%，且十分位曲线单调，**(b) 的负值判读被推翻**：模型对"涨"确有排序力，只是数值（BSS/校准）上偏。最高十分位完整三分类分布：plain 预测 up 最高档 down 42.3% / flat 16.2% / up 41.5%（up 相对 down 的领先优势比全市场平均还差）；残差标签同档 down 28.2% / flat 40.4% / up 31.3%（领先优势恢复，但 flat 占比最大）。按天聚类自助法 CI（1000 次重抽样）：三个类别、两套标签的 top-base 提升度绝大多数 95% CI 不跨 0（仅 plain 的 down 类下界贴 0）。

**覆盖率分母（核实后可用重叠口径）**：34 个"有数据但零样本贡献"的下架合约逐一核实结算日，**34/34 全部确认**与决策窗口（2025-10-01 起）无重叠（33 个结算日早于窗口起点，1 个 AERGOUSDT 无真实结算日但数据本身在窗口前结束）。裁定：门禁要断言的是"训练集内不存在实质幸存者偏差"，一个在窗口开始前就已死亡的合约根本不在窗口的宇宙里，收不收它不影响窗口内的幸存者偏差，正确分母应为"存活期与窗口重叠的已识别下架合约"（81/87≈93.1%），而非全部 121 个（66.9%）。**这是第二次在门禁失败之后修改定义**（第一次是 residualBss）：修改发生在看到 66.9% 未达标之后；理由可独立于结果检验（窗口外合约不属于该窗口的宇宙）；两种口径数字均留档（121 分母 66.9% / 87 分母 93.1%）。

**未回测声明**：T34/T35 均为描述性拆分与敏感性检验，不含任何上线模型或收益结论。

### T36 · "会动/不会动"二分类重述（2026-09-23，探索性，架构提出）

按类别拆分的负值（T35(b)）启发了一个重述假设：模型区分的是"会不会动"，不是"往哪动"——预测 up 与预测 down 的最高十分位，flat 占比都比基础率暴跌（29.2%→16.2%/20.0%），而预测 flat 的最高十分位 flat 占比暴涨到 44.1%。**这个假设是看过数据之后才想到的，必须当新假设直接测，不能靠重新解读既有数字**。

二分类标签（flat→0/not_moved，down 或 up→1/moved），同一套 21 特征、同一套折、同一套权重，只换标签定义，不调参：池化 BSS 0.0326（三分类是 0.0072-0.0077），95% CI [0.0161, 0.0481] 完全在零以上；ECE 0.0074（三分类 0.0538，好 7 倍）；十分位提升度单调，最高档 moved 实际率 85.3%（base rate 70.8%）。**标注为探索性**：测试折已被反复查看十余轮，需 2026-09-23 之后新采集数据的前瞻验证才能作为上线依据，不能直接进门禁。

### T37 · 嵌套特征集对比：1 特征 vs 3 特征 vs 21 特征（2026-09-23，架构预登记判读规则）

ECE 大幅改善（0.0538→0.0074）让"能否诚实显示概率"第一次成立，但也带来一个疑问：`moved` 本质是波动率预测，波动率聚集是金融数据最古老的规律之一，一个只看当前 ATR 分位（`d1_vol_squeeze_pct`）的朴素规则可能就拿到了大部分成绩，若真如此，18 个结构类特征（含架构挑选保留的 F 组 SMC/Wyckoff/缠论衍生特征）全是装饰品。

同一套折/标签/权重/训练设置，只换特征列：基线A（仅 d1，1 特征）BSS 0.0390，CI [0.0287,0.0493]；基线B（d1+d3+d4 压缩组，3 特征）BSS 0.0365，CI [0.0251,0.0483]；完整（21 特征）BSS 0.0326，CI [0.0163,0.0481]。点估计**单调递减**（21 特征反而最低），三者 CI 高度重叠（不能断言"完整更差"，但也不能断言更好）。十分位跨度同样单调递减（32.3pp → 30.5pp → 29.2pp）。判读：**其余 20 个特征（含 F 组）对判别力没有可测量的增量**。独立佐证：门禁本有"有效样本数 ≥ 20×自由参数"的充足性规则，代入按天聚类算出的约 360 个有效样本，21 特征二分类需要约 440，不够；3 特征需要 80，够；1 特征需要 40，远够——**这条规则是看到任何结果之前定的，独立预言了现在看到的方向**。唯一单调改善的是 ECE（0.0166→0.0170→0.0074），完整模型的优势在校准，不在判别力。

**未回测声明**：以上为相对贡献的同数据对比，合法（不需要前瞻验证），但效应本身是否真实仍受 T36 的探索性标注约束。

### T38 · 校准方法对比：等渗回归、Platt 缩放（2026-09-23，架构预登记分支，T22 早先约定的"仅 ECE 不过才上 Platt/isotonic"条件在此实质满足）

若"基线A + 校准"能追上完整模型的 ECE 同时不损失 BSS，21 个特征、6 条规则、十几个冻结参数的大部分复杂度可能都不必要。

等渗回归：在每折训练集里按时间顺序切出最后 20% 的天数作校准验证集（从不碰测试折），PAVA 拟合。结果：BSS 0.0106，CI [−0.0123,0.0334]（跨零），ECE 0.0288（测试集，比基线A 原始 ECE 0.0166 更差）；十分位曲线出现不单调的反转点。Platt 缩放（logistic，2 参数，同一个校准验证集上拟合，仅试一次）：BSS 0.0227，CI [0.0002,0.0417]（下界贴零），ECE 0.0181，同样没有优于基线A 原始 ECE。按预登记规则："若 Platt 也不能在保住 BSS 的同时把 ECE 压到明显低于基线A，结论即为不做校准，不再试第三种。"——到此为止。

### T39 · 校准变差的两个混淆排查（2026-09-23，架构要求，排除数据量混淆并诊断过拟合）

**混淆一**：等渗/Platt 的校准验证集切分让训练只用了 80% 的折内数据，BSS 下降有多少来自校准本身、多少只是训练数据变少，原本分不清。对照：基线A 仅用同样的 80% fit 划分、不做任何校准，BSS 0.0393，CI [0.0298,0.0500]，ECE 0.0166——与 100% 数据版本（BSS 0.0390，ECE 0.0166）几乎完全一致。**"数据变少"混淆被排除**，两种校准方法的成绩下滑确系校准本身导致。

**混淆二（过拟合判据）**：等渗校准的校准集自身 ECE（样本内）0.0119，测试集 ECE（样本外）0.0288，约 2.4 倍——存在过拟合迹象，但不是"校准集≈0、测试集很差"的教科书式极端案例。

**Platt 为什么也伤 BSS，本身是个发现**：Platt 是单调变换，理论上不该改变排序/判别力，但 BSS 仍从 0.0393 掉到 0.0227（第 0 折甚至 −0.0124）。唯一说得通的解释：**分数与结果的映射关系随时间漂移**——校准集是测试期前面 20% 的日子，那段拟合出的映射到测试期已不适用。结论：**这份数据里，校准不跨时间迁移**，反过来支持"面板显示排名而非绝对概率"这个方向（排名只要求单调性，单调性稳；概率数值要求映射稳定，映射不稳）。

### T40 · 模型排序 ≡ 原始 d1 排序；方向确认（2026-09-23，本轮探索线的最后一项检验）

单特征逻辑回归是该特征的单调变换，排序只取决于单调性，因此"训练模型"这一步理论上不该比"直接按 d1 排序"多出任何判别力——需要测，不能假定。

结果：基线A 模型十分位曲线（86.3|81.6|77.5|74.4|72.0|69.4|66.1|65.4|61.6|54.0）与直接按原始 d1 **升序**排序的曲线（86.6|81.5|77.3|74.5|71.9|69.5|66.0|65.6|61.5|54.0）逐点相差不超过 0.3 个百分点，实质等价。方向：d1 **降序**（最高 d1 在前）给出反向曲线，确认相关方向是 **d1 越低（波动越压缩）→ 越可能触发异动**（"会动"，任一方向），提升度 +15.7pp，95% CI [13.7pp, 17.7pp]，稳固。**这验证了项目最初"蓄势之后启动"的核心假设，但捕捉它只需要一个波动率分位数，不需要训练、不需要模型产物、不需要校准。**

**T34-T40 汇总（"加东西、变差"的四次独立观测，判读交给 `docs/findings-v1.md`）**：21 特征(0.0326) < 3 特征(0.0365) < 1 特征(0.0390)；1 特征+等渗(0.0106) < 1 特征；1 特征+Platt(0.0227) < 1 特征；模型排序 ≡ 原始 d1 排序。每一次都是先加、预先写死判读规则、诚实测量、按规则撤回。

**未回测声明**：T36-T40 全部为探索性发现，测试折被反复查看十余轮，均需 2026-09-23 之后新采集数据的前瞻验证，不构成上线依据。

---

## 未回测声明

本台账中的数据与统计均为**特征侧的描述性测量**，没有任何一条经过标签、收益或概率校准的检验。区间判别参数由此选定，并不代表该特征具有预测力——那要等 P3 的校准管线来回答。
